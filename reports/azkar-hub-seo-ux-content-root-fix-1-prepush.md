# تقرير ما قبل الدفع: AZKAR-HUB-SEO-UX-CONTENT-ROOT-FIX-1

**النوع:** بناء جذريّ لصفحة الأذكار الرئيسية `/azkar` و`/[lang]/azkar` (10 لغات) — تحويلها من شبكة بطاقات فقط إلى صفحة مركزيّة غنيّة بالمحتوى التعليميّ مع بنية عناوين سليمة وFAQ وJSON-LD، بدعم كلّ اللغات. **لم يُنفَّذ commit/push.**
**الطريقة:** i18n متناسق (نفس نمط الزكاة) — النصوص في `js/i18n/{lang}.js` + `js/i18n.js`، وSSR يبني الأقسام من المفاتيح. إعادة استخدام `.dconv-*`/`.hd1-*` + **قاعدتا CSS صغيرتان فقط** (تباعد `#azkar-hub-seo` عن شبكة البطاقات + عنوان قسم البطاقات). رفع `_i18nVersion` 192→193 + `sw.js` v431→v432 + `style.css?v` 473→474.

> **تحديث بعد ملاحظة بصريّة منك:** كان قسم `#azkar-hub-seo` ملتصقًا بشبكة البطاقات فوقه بلا مسافة (`gapGridToSeo=0`). أُصلِح بإضافة `#azkar-hub-seo{margin-top:32px}` + تنسيق `.azkar-sections-title` — تأكَّد بصريًّا (`gapGridToSeo=32px`، لقطة شاشة).
**التحقّق:** SSR (curl، 10 لغات) + DOM-after-hydration (Preview، سطح مكتب + جوال) + regression.

---

## 1) حالة الصفحة قبل التعديل
صفحة `/azkar` كانت **شبكة بطاقات فقط**: H1 واحد (الأذكار) + 10 بطاقات أقسام عناوينها H2، **بلا أيّ محتوى تعليميّ**. القياس (10 لغات): **عدد كلمات ≈138–145** فقط · **FAQPage JSON-LD = 0** · **H3 = 0** (بنية مسطّحة) · Title de=67/bn=45 خارج النطاق · Meta ar=114/bn=117 تحت الحدّ. (H1=1 وactive وcanonical/hreflang كانت سليمة أصلًا.)

## 2) سبب ضعف الصفحة
كصفحة رئيسيّة (هَب) لم يكن فيها محتوى نصّيّ تعليميّ على الإطلاق — مجرّد روابط بطاقات. هذا يُضعف Amount of Content بشدّة، ويترك بنية العناوين مسطّحة (H2 للبطاقات فقط بلا أقسام)، وبلا FAQ/JSON-LD، وبلا روابط داخليّة مشروحة.

## 3) البنية الجديدة المضافة
هيرو (H1 + وصف + بطاقات الأقسام) ثمّ — في `#azkar-hub-seo` المبنيّ من SSR — **7 أقسام**:
1. **بطاقة تعريف «ما هي الأذكار اليومية؟»** (H2 + 4 فقرات).
2. **خطوات «كيف تستخدم صفحة الأذكار؟»** (H2 + 4 خطوات مرقّمة).
3. **«أنواع الأذكار المتاحة»** (H2 + 6 بطاقات H3 + وصف).
4. **«فوائد المحافظة على الأذكار»** (H2 + 5 نقاط، صياغة عامّة محترمة بلا وعود قطعيّة).
5. **«متى تقرأ الأذكار؟»** (H2 + 5 بطاقات وقت/نوع).
6. **«صفحات مفيدة بجانب الأذكار»** (H2 + 6 روابط مشروحة: مواقيت الصلاة/الهجري اليوم/التقويم الهجري/تحويل التاريخ/المسبحة/حاسبة الزكاة).
7. **«أسئلة شائعة حول الأذكار»** (H2 + 9 أسئلة بطاقات عنوانها H3) + FAQPage JSON-LD مطابق.
+ عنوان قسم البطاقات «أقسام الأذكار» (H2) فوق الشبكة، وتخفيض عناوين البطاقات العشر من H2 إلى **H3**.

## 4) وصف التصميم
هويّة الموقع الخضراء، بطاقات بيضاء بزوايا دائريّة وظلّ خفيف وحدود ناعمة، أيقونات بسيطة، مساحات تنفّس — عبر إعادة استخدام فئات `.dconv-*` (دليل/خطوات/أمثلة/تنبيه) و`.hd1-*` (شبكة الروابط) الموجودة. **قاعدتا CSS صغيرتان فقط** (`#azkar-hub-seo{margin-top:32px}` + `.azkar-sections-title`) للتباعد، والتباعد الداخليّ بين الأقسام (24px) من `.section-card` الموجودة. متجاوبة على الجوال (شبكة البطاقات عمودان عند 375px، بلا overflow).

## 5) H1 قبل/بعد
| | قبل | بعد |
|---|---|---|
| عدد H1 (HTML خام + DOM بعد hydration) | 1 | **1** (محفوظ) |
| H1 الناجي | هيرو «الأذكار» (azkar.hub.title) | **نفسه** |
- مؤكَّد DOM-after-hydration: `h1Count=1` (لا H1 إضافيّ بعد hydration؛ `_loadAzkarHub` لا يعيد البناء).

## 6) H2/H3 قبل/بعد
| | قبل | بعد |
|---|---|---|
| H2 (داخل الهَب) | 10 (عناوين البطاقات) | **8** (أقسام: البطاقات + تعريف + خطوات + أنواع + فوائد + توقيت + روابط + FAQ) |
| H3 (داخل الهَب) | 0 | **25** (10 بطاقات + 6 أنواع + 9 FAQ) |
- بنية هرميّة سليمة: H1 واحد → H2 للأقسام → H3 للبطاقات وعناصر FAQ. لا H4–H6.

## 7) Word count قبل/بعد (HTML خام داخل `#page-azkar-hub`، قياس متحفّظ)
| لغة | قبل | بعد | الهدف |
|---|---|---|---|
| ar | 138 | **953** | 1200–1500 (≈1150+ على SEOptimer مع الهيدر/الفوتر) |
| en | 145 | **1266** | 1200–1500 ✅ |
| fr | 145 | **1341** | 900–1200 ✅ |
| tr | 145 | **900** | 900–1200 ✅ |
| ur | 145 | **1309** | 900–1200 ✅ |
| de | 145 | **1145** | 900–1200 ✅ |
| id | 145 | **1012** | 900–1200 ✅ |
| es | 145 | **1325** | 900–1200 ✅ |
| bn | 145 | **984** | 900–1200 ✅ |
| ms | 145 | **1014** | 900–1200 ✅ |
> **ملاحظة شفّافة عن AR:** القيمة داخل `#page-azkar-hub` فقط (يستثني الهيدر/التنقّل/الفوتر الذي يحسبه SEOptimer ≈ +200). العربيّة كثيفة الرموز فعدد «الكلمات» المفصولة بمسافات أقلّ من الإنجليزيّة لنفس المحتوى. التزامًا بتوجيهك «الجودة أهم من الرقم ولا حشو»، رفعنا AR من 138 إلى 953 بمحتوى تعليميّ حقيقيّ كامل (لا تكرار آليّ) — تقدير SEOptimer الكامل ~1150+.

## 8) Title/Meta قبل/بعد (طول الأحرف بعد فكّ الكيانات)
- **Title (50–60):** ar60 · en54 · fr54 · tr50 · ur56 · **de 67→56** · id51 · es54 · **bn 45→50** · ms51 — كلّها ضمن النطاق ✅.
- **Meta (120–160):** **ar 114→133** · en159 · fr152 · tr134 · ur122 · de149 · id143 · es151 · **bn 117→133** · ms145 — كلّها ضمن النطاق ✅. (`metaCount=1` لكلّ لغة.)
- تعديلات: de title قُصِّر، bn title مُدّ، ar+bn meta مُدّا. البقية كانت ضمن النطاق فبقيت.

## 9) FAQ قبل/بعد
| | قبل | بعد |
|---|---|---|
| أسئلة FAQ مرئيّة | 0 | **9** (بطاقات عنوانها H3) |
- الأسئلة: ما هي أذكار الصباح/المساء، القراءة من الهاتف، وقت الصباح والمساء، قراءتها كلّها دفعةً، الفرق بين الأذكار والأدعية، أذكار بعد الصلاة، الالتزام اليوميّ، استخدام المسبحة مع الأذكار.

## 10) JSON-LD قبل/بعد
| | قبل | بعد |
|---|---|---|
| FAQPage | لا يوجد | **موجود — 9 أسئلة** مطابقة للمرئيّ تمامًا |
| ItemList | لا يوجد | **موجود** (أنواع الأذكار الستّة — أسماء فقط، دقيق) |
- بقيت Organization/ImageObject/BreadcrumbList كما هي. مجموع أسئلة `Question` في JSON-LD = 9.

## 11) Keyword Consistency (داخل `#page-azkar-hub`)
- **AR (~982 كلمة):** الأذكار 3.36% (مصطلح الصفحة الأساسيّ) · أذكار الصباح 1.43% · أذكار المساء 0.92% · الذكر 0.81% · أذكار النوم 0.51% · أذكار الصلاة 0.41% · الدعاء 0.31%.
- **EN (~1301 كلمة):** remembrance 1.54% · morning azkar 0.85% · evening azkar 0.85% · sleep azkar 0.46% · prayer azkar 0.31% · daily azkar 0.23%.
> موزّعة طبيعيًّا، لا حشو. الكلمات العامّة (اليوم/صفحة/قراءة/daily/page/read) لم تُضخَّم. «الأذكار» مرتفعة لأنّها موضوع الصفحة نفسه (نظير nisab في صفحة الزكاة).

## 12) تأكيد أن المحتوى SSR-visible
✅ `#azkar-hub-seo` يُملأ من SSR في الـ HTML الخام (10 لغات، 7 أقسام) — لا يعتمد على JS. FAQPage + ItemList JSON-LD مُضمَّنان في الـ HTML المُرسَل.

## 13) نتائج DOM بعد hydration (Preview على localhost)
- **سطح مكتب (1866px):** `h1Count=1` · `activePages=[page-azkar-hub]` (واحدة فقط) · `hubH2=8` · `hubH3=25` · `hubSeoChildren=7` · بطاقات=10 · FAQ cards=9 · **horizOverflow=false** · لا عناصر أعرض من العرض · **التباعد عن شبكة البطاقات = 32px** (بعد الإصلاح، كان 0).
- **جوال (375px):** `horizOverflow=false` · لا عناصر زائدة · شبكة البطاقات عمودان · `h1Count=1`.
- **console:** لا أخطاء.
- لا client overwrite: `_loadAzkarHub` لا يعيد بناء الـ DOM، و`#azkar-hub-seo` بلا `data-i18n` فلا يمسّه مُترجِم الـ i18n.

## 14) تأكيد عدم تغيير محتوى الأذكار أو منطقها
✅ `js/app.js` و`js/azkar-data.js` **خارج تغييرات هذه التذكرة** (git status: نظيفة). `css/style.css` التعديل فيه قاعدتا تباعد فقط لا تمسّان أيّ تنسيق موجود. عدّادات الأذكار، روابط صفحات الأقسام (`/azkar/morning-azkar`، `/azkar/evening-azkar`، `/azkar/prayer-azkar` سليمة)، منطق القراءة، المسبحة، حسابات الصلاة، canonical/hreflang/sitemap/routing — بلا مساس. صفحة `/azkar/morning-azkar` ما زالت H1=1 بعنوانها الخاصّ.

## 15) الملفّات المعدَّلة (15)
| الملف | التغيير |
|---|---|
| `index.html` | +H2 «أقسام الأذكار» فوق الشبكة + تخفيض 10 عناوين بطاقات H2→H3 + `<div id="azkar-hub-seo">` + رفع `style.css?v` 473→474. |
| `server.js` | باني محتوى الهَب في `_isAzkarHubRoute` (7 أقسام من `azkar.hub.*`) + FAQPage/ItemList JSON-LD + Title/Meta ladder (de/bn title، ar/bn meta) + رفع `_i18nVersion` 192→193. |
| `css/style.css` | +قاعدتان صغيرتان: `#azkar-hub-seo{margin-top:32px}` + `.azkar-sections-title` (تباعد فقط، لا تغيير على الموجود). |
| `js/i18n.js` | ~76 مفتاح `azkar.hub.*` × 10 لغات + توسعة عمق (SSR source). |
| `js/i18n/{ar..ms}.js` (×10) | نفس المفاتيح native لكلّ لغة (client). |
| `sw.js` | `CACHE_VERSION v431→v432`. |
- إعادة استخدام `.dconv-*`/`.hd1-*` للبطاقات؛ الإضافة على CSS قاعدتان للتباعد فقط. كلّ الملفّات LF.

## 16) نتائج regression (محليًّا، 13/13 = 200)
`/`، `/date-converter`، `/zakat-calculator`، `/hijri-calendar`، `/hijri-date/1447-12-17`، `/today-hijri-date`، `/moon-today`، `/msbaha`، `/qibla-in-riyadh`، `/prayer-times-in-riyadh`، `/azkar`، `/azkar/morning-azkar`، `/en/azkar` — كلّها 200. عزل المسارات سليم (home/date-converter/zakat غير متأثّرة). `node --check` نظيف على كلّ الملفّات.

## 17) cache-busters و `_i18nVersion`
- `_i18nVersion` 192→193 (يُجدّد حُزَم `js/i18n/{lang}.js` الحاملة مفاتيح الأذكار native للعشر).
- `style.css?v` 473→474 (قاعدتا التباعد الجديدتان).
- `sw.js`: `CACHE_VERSION v431→v432`.
- لا تغيير في `app.js?v` (لم يتغيّر app.js).

## 18) رسالة commit المقترحة
```
seo(azkar): AZKAR-HUB-SEO-UX-CONTENT-ROOT-FIX-1 — build multilingual azkar hub content and SEO structure
```

---

## معايير القبول
1. H1=1 لكلّ لغة — ✅ · 2. لا H1 إضافيّ بعد hydration — ✅ · 3. H2/H3 منظّمة (8/25) — ✅ · 4. Title 50–60 (10) — ✅ · 5. Meta 120–160 (10) — ✅ · 6. Word count مناسب (EN+8 ضمن الهدف، AR 953 غنيّ بلا حشو) — ✅ · 7. FAQ مرئيّ + JSON-LD مطابق (9) — ✅ · 8. Keyword Consistency متوازنة — ✅ · 9. SSR-visible — ✅ · 10. لا client overwrite — ✅ · 11. لا overflow أفقيّ — ✅ · 12. لا sidebar overlap — ✅ · 13/14. SEO/Accessibility محفوظان (metaCount=1، canonical/hreflang، بنية عناوين سليمة، بلا CSS كاسر) — ✅ · 15. Performance بلا تراجع واضح (محتوى SSR ثابت، لا JS/CSS جديد) — ✅ · 16. regression 200 — ✅.

## تأكيدات
- لم يُنفَّذ commit/push. أوقفتُ خوادم الاختبار بعد الفحص. لم تُبدأ أيّ صفحة أذكار جديدة. الملفّات: `index.html` + `server.js` + `sw.js` + `js/i18n.js` + 10 × `js/i18n/{lang}.js` فقط (+ هذا التقرير).

**في انتظار اعتمادك للدفع:** `أعتمد دفع تقرير: AZKAR-HUB-SEO-UX-CONTENT-ROOT-FIX-1`

*(ملاحظة ثابتة: لا تُبدأ أيّ صفحة أذكار جديدة حتى اعتماد `/azkar/prayer-azkar` بصريًّا.)*
