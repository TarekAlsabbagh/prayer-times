# تقرير ما قبل الدفع: DATE-CONVERTER-PAGE-UX-SEO-CONTENT-FIX-1-CLEANUP-1

**النوع:** تنظيف UI — حذف زرّ CTA مكرَّر من بطاقة أداة تحويل التاريخ. **لم يُنفَّذ commit/push.**
**متابعة لـ:** `DATE-CONVERTER-PAGE-UX-SEO-CONTENT-FIX-1` (`eaf4c16`).

## 1) المطلوب
حذف `<p class="conv-today-cta">` (زرّ «التاريخ الهجري اليوم» القديم) من **داخل** بطاقة الأداة في `#page-date-converter` بشكل كامل.

## 2) السبب
أصبح الزرّ **مكرَّرًا**: شبكة الروابط الجديدة (`#dconv-quicklinks` بتصميم `.hd1-tools-section`) تتضمّن بطاقة «التاريخ الهجري اليوم» كأوّل بطاقة. فوجوده داخل بطاقة الأداة تكرار بصريّ لا حاجة له.

## 3) ما تم تعديله
| الملف | التغيير |
|---|---|
| `index.html` | حذف كتلة `<p class="conv-today-cta"><a …/today-hijri-date …>…</a></p>` (+ تعليق HD-2 القديم) من بطاقة الأداة؛ استُبدلت بتعليق توثيقيّ. (−6/+4) |
- **ملفّ واحد فقط.** لا server.js، لا CSS، لا app.js. (`index.html` يُخدَم no-cache فلا حاجة cache-buster.)

## 4) التحقّق (محليًّا، داخل `#page-date-converter`)
| الفحص | النتيجة |
|---|---|
| `conv-today-cta` داخل الكتلة | **0** (محذوف) ✅ |
| `btn btn-outline` (الزرّ القديم) داخل الكتلة | **0** ✅ |
| بطاقة «التاريخ الهجري اليوم» في الروابط | **1** (الرابط ما زال متاحًا عبر البطاقة) ✅ |
| تبويبات الأداة | **سليمة** (3 تبويبات + الحاوية) ✅ |
| H1 | **1** (دون تغيير) ✅ |
| `/en/dateconverter` | **200** ✅ |
| منطق التحويل | **دون مساس** (لم تُلمَس الحقول/الأزرار/الدوالّ) |

## 5) تأكيدات
- لم يُنفَّذ commit/push. لم تُمَسّ منطق التحويل/الحسابات/canonical/hreflang/sitemap/routing/الأذكار. لا فقدان للرابط (متاح عبر بطاقة الروابط السريعة). أوقفتُ خادم الاختبار.

## 6) رسالة commit المقترحة
```
chore(ui): DATE-CONVERTER-PAGE-UX-SEO-CONTENT-FIX-1-CLEANUP-1 — remove redundant today-hijri CTA from converter card
```

**في انتظار اعتمادك للدفع:** `أعتمد دفع تقرير: DATE-CONVERTER-PAGE-UX-SEO-CONTENT-FIX-1-CLEANUP-1`

*(ملاحظة ثابتة: لا تُبدأ أيّ صفحة أذكار جديدة حتى اعتماد `/azkar/prayer-azkar` بصريًّا.)*
