# تقرير ما قبل الدفع: DATE-CONVERTER-RESULT-DUAL-CALENDAR-DISPLAY-1

**النوع:** عرض فقط — السطر البارز لنتيجة التحويل يعرض الآن **التقاويم الثلاثة في سطر واحد بفواصل** (هجري · ميلادي · شمسي) في كلّ التبويبات + توحيد تسمية النتيجة إلى «التواريخ المقابلة». **لم يُنفَّذ commit/push.**
**التحقّق:** DOM (Preview، بعد hydration) للتبويبات الثلاثة + AR/EN + SSR/served-assets (curl) + node --check (13 ملفّ JS) + regression.

---

## 1) ما تم تغييره
1. **السطر البارز** في `buildConvSummaryHTML` (js/app.js): كان يعرض **تاريخًا واحدًا** حسب التبويب → أصبح **سطرًا موجزًا للتقاويم الثلاثة** `${dayName} <هجري> · <ميلادي> · <شمسي>` (اسم اليوم مرّة واحدة، كلّ تاريخ في `<span class="conv-day-seg">` nowrap، الفاصل `<span class="conv-day-sep"> · </span>`).
2. **تسمية النتيجة** `.converter-result-label`: وُحِّدت الثلاث إلى «التواريخ المقابلة»/مقابلها بدل «التاريخ الهجري المقابل»/… (صارت مضلِّلة لأنّ السطر يعرض الثلاثة).
3. **الجدول التفصيليّ لم يتغيّر.**

## 2) السبب الجذريّ لظهور التسمية القديمة (وكيف عولج)
الترجمات الفعليّة وقت التشغيل تُحمَّل من **ملفّات اللغة المنفصلة `js/i18n/{lang}.js`** (يستبدل server.js سطر `js/i18n.js` بـ `i18n-core.js?v=N` + `i18n/{lang}.js?v=N`، حيث `N=_i18nVersion`). أي تعديلي الأوّل على `js/i18n.js` (الـ fallback غير المُحمَّل عبر SSR) لم يظهر. الحلّ الصحيح: تعديل الـ **10 ملفّات `js/i18n/{lang}.js`** + رفع `_i18nVersion` في server.js من `190→191` ليُجبَر العملاء على جلب الملفّات الجديدة. **تأكيد بعد hydration:** `t('converter.result_hijri')` = «التواريخ المقابلة»، والتسميات الثلاث في التبويبات = «التواريخ المقابلة».

## 3) أمثلة قبل/بعد (السطر البارز)
| التبويب | قبل | بعد |
|---|---|---|
| من الميلادي | «الأربعاء 17 ذو الحجة 1447 هـ» (هجري فقط) | «الأربعاء 17 ذو الحجة 1447 هـ · 3 يونيو 2026 م · 13 جوزا 1405 ش» |
| من الهجري | «الأربعاء 3 يونيو 2026 م» (ميلادي فقط) | نفس السطر الموجز |
| من الشمسي | «الأربعاء 13 جوزا 1405 ش» (الإدخال!) | نفس السطر الموجز |
- **EN:** «Wednesday 17 Dhu al-Hijjah 1447 AH · 3 June 2026 · 13 Khordad 1405 SH».

## 4) نتائج التبويبات الثلاثة + AR/EN (DOM، بعد hydration)
✅ الثلاثة: السطر = 3 مقاطع + فاصلان، التسمية = «التواريخ المقابلة»، التحويل صحيح. تنسيق `.conv-summary-day`: `font-weight:700`, `color:var(--primary)` أخضر, `text-align:center`, + `conv-day-seg{white-space:nowrap}` + التفاف للسطر في الجوّال. ✅ **EN** السطر والترجمة سليمان.

## 5) تأكيد عدم تغيير الحسابات
✅ **صفر مساس.** `HijriDate.toHijri/toGregorian`، `gregorianToJalali/jalaliToGregorian`، `convertToHijri/convertToGreg/convertFromSolar` لم تُمَسّ. اختبار حيّ: 2026-06-03 → 17 ذو الحجة 1447 + 13 جوزا 1405.

## 6) تأكيد عدم تغيير SEO
✅ Title/Meta/H1/FAQ/JSON-LD/canonical/hreflang/sitemap **لم تُمَسّ**. تغيير server.js محصور في **قيمة `_i18nVersion` (190→191)** — رقم نسخة كاش حُقَن في وسم سكربت i18n فقط، **لا منطق توجيه/SEO**. لا أثر على الأذكار.

## 7) الملفّات المعدَّلة (16)
| الملف | التغيير | السبب |
|---|---|---|
| `js/app.js` | `buildConvSummaryHTML`: السطر → 3 تقاويم بفواصل | جوهر التذكرة |
| `css/style.css` | `.conv-summary-day` (1.05rem/700/center) + `.conv-day-seg`/`.conv-day-sep` | تنسيق السطر + التفاف الجوّال |
| `js/i18n/{ar,en,fr,tr,ur,de,id,es,bn,ms}.js` (10) | تسمية النتيجة الـ3 → «التواريخ المقابلة»/مقابلها | **ملفّات الترجمة الفعليّة وقت التشغيل** |
| `server.js` | `_i18nVersion '190'→'191'` | كسر كاش ملفّات اللغة حتى تظهر التسمية الجديدة للعملاء |
| `js/i18n.js` | نفس التوحيد (fallback غير-SSR) | اتّساق مع ملفّات اللغة |
| `index.html` | 3 تسميات SSR ثابتة → «التواريخ المقابلة» + cache-busters (app 760→761، css 470→471، i18n.js-marker 190→192) | تسمية AR صحيحة من أوّل رسم + كسر كاش app.js/css |
| `sw.js` | `CACHE_VERSION v422 → v424` | علامة نشر |
- **لا تغيير في منطق التحويل، لا أذكار.**

## 8) regression + node --check
✅ **node --check 13/13 ملفّ JS** (app.js/i18n.js/server.js + 10 ملفّات لغة) OK. ✅ **regression 200**: `/`، `/date-converter`+(en/fr)، `/azkar`، `/moon-today`. لا أخطاء console.

## 9) رسالة commit المقترحة
```
feat(date-converter): DATE-CONVERTER-RESULT-DUAL-CALENDAR-DISPLAY-1 — show compact multi-calendar result summary
```

---

## معايير القبول
1. النتيجة سطر واحد بفواصل — ✅ · 2. الجدول التفصيليّ كما هو — ✅ · 3. الحسابات لا تتغيّر — ✅ · 4. التبويبات الثلاثة تعمل — ✅ · 5. لا ينكسر في الجوّال — ✅ · 6. لا أخطاء console — ✅ · 7. regression 200 — ✅ · 8. لا أثر SEO — ✅ · 9. التسمية تظهر صحيحة بعد hydration (التواريخ المقابلة) — ✅.

## تأكيدات
- لم يُنفَّذ commit/push. أوقفتُ خادم الاختبار. عمل MULTILANG محفوظ في `git stash`. لم تُبدأ أيّ صفحة أذكار.

**في انتظار اعتمادك للدفع:** `أعتمد دفع تقرير: DATE-CONVERTER-RESULT-DUAL-CALENDAR-DISPLAY-1`

*(ملاحظة ثابتة: لا تُبدأ أيّ صفحة أذكار جديدة حتى اعتماد `/azkar/prayer-azkar` بصريًّا.)*
