# تقرير ما قبل الدفع: HIJRI-NEW-YEAR-COUNTDOWN-SEO-CONTENT-H1-FIX-1

**النوع:** إصلاح SEO جذريّ لعائلة صفحات العدّ التنازليّ الأربع (`ramadan-countdown` · `eid-al-fitr-countdown` · `eid-al-adha-countdown` · `hijri-new-year-countdown`) — عنوان ذكيّ ديناميكيّ، وصف ميتا، **H1 واحد فعليّ في SSR**، محتوى تعليميّ غنيّ بكل اللغات العشر، FAQ مرئيّ + JSON-LD مطابق. **مخصّص لكل مناسبة، بلا حشو، مع الحفاظ على التصميم والعدّاد والحسابات.**
**الحالة:** لم يُدفع — بانتظار اعتمادك. **القاعدة:** `HEAD = origin/main = b573366`.
**النطاق:** صفحات العدّ الأربع فقط — لم تُمَسّ صفحات prayer-times/qibla/moon/azkar/zakat/date-converter/المدن.

---

## 1) اسم التذكرة
**HIJRI-NEW-YEAR-COUNTDOWN-SEO-CONTENT-H1-FIX-1**

## 2) الأسباب الجذريّة (قبل)
| المشكلة | السبب |
|---|---|
| **Title = تسريب الصفحة الرئيسة** (62 AR / 65 EN) على الصفحات الأربع | لا يوجد `staticPages` ولا فرع في `buildSeoForPath` للعدّ |
| **Meta = تسريب الرئيسة** (114 AR / 134 EN) | نفس السبب |
| **H1 = 0** على `eid-al-fitr` و`hijri-new-year` | الـ`data-i18n` على `<span>` داخليّ لا على وسم `<h1>` ⇒ `_downgradeInactiveH1s` يقرأ سمات الوسم نفسه فلا يطابق ويُنزِّل الـH1 إلى `<h2>` |
| **FAQ غير مرئيّ في SSR** | `#*-faq-list` يملؤه الـJS فقط (فارغ في SSR)، بلا FAQPage JSON-LD |
| **محتوى قليل** (~278–372 كلمة) | الصفحة نشطة على الرئيسة (`#page-prayer-times`)، والعدّ مخفيّ، والجدول والـFAQ فارغان في SSR |

## 3) الحلّ (بعد) — معماريّة مطابقة لأنماط مُثبتة (zakat / country / isTodayHijriHub)
1. **H1 واحد**: أُضيف `id="cd-h1-{occ}"` لوسوم `<h1>` الأربعة في index.html، وحُوِّلت علامات `_getActiveH1Marker` من `kind:'i18n'` إلى `kind:'id'` ⇒ يُبقي الـH1 الصحيح ويُنزِّل بقيّة الـ~19 إلى `<h2>`.
2. **تفعيل SSR**: داخل `serveHtmlWithSeo` يُفعَّل `#page-{occ}-countdown` ويُلغى تفعيل قشرة الرئيسة `#page-prayer-times` (مطابق لكتلة zakat).
3. **عنوان ديناميكيّ**: فرع جديد في `buildSeoForPath` يبني العنوان من سلّم `{long, medium, short}` ويُعوّض `{hy}` (السنة الهجريّة المستهدَفة) و`{gy}` (السنة الميلاديّة) ثمّ يختار **الأطول الذي يقع في ~[50,60]**، وإلا الأطول ≤60، وإلا الأقصر — **بلا قصّ**.
4. **Meta** 120–160 لكل لغة (مع `{hy}`/`{gy}`).
5. **FAQ مرئيّ**: تُحقَن أسئلة i18n العشرة (`{prefix}.faq_q/a 1..10`) في `#*-faq-list` مع تعويض `{n}`/`{date}`/`{hyear}`، ويُصدَر **FAQPage JSON-LD واحد مطابق نصًّا** للمرئيّ.
6. **محتوى تعليميّ**: 4 أقسام H2 لكل مناسبة×لغة من `data/countdown-seo.js` تُحقَن في `#cd-edu-{occ}` (تُكمِّل ولا تُكرِّر قسم المعلومات الدينيّ الموجود).

**حساب الهدف الديناميكيّ**: `_nextCountdownTarget(ev)` يستخدم `_hijriNow()` + `_hijriToGregorian()` لإيجاد أقرب وقوع قادم لكل مناسبة (يتقدّم تلقائيًّا للسنة التالية بعد انقضاء الموعد) ⇒ العنوان والمحتوى يبقيان صحيحَين عبر السنين دون تدخّل (حاليًّا hijri-ny=1448/2026، ورمضان/العيدان=1448/2027).

## 4) جدول العناوين — الطول النهائيّ بعد تعويض السنة (كلّها داخل [50,60] ✓)
| اللغة | hijri-ny | ramadan | eid-fitr | eid-adha |
|---|---|---|---|---|
| ar | 60 ✓ | 53 ✓ | 51 ✓ | 51 ✓ |
| en | 60 ✓ | 59 ✓ | 55 ✓ | 60 ✓ |
| fr | 59 ✓ | 57 ✓ | 60 ✓ | 60 ✓ |
| tr | 58 ✓ | 56 ✓ | 58 ✓ | 52 ✓ |
| ur | 56 ✓ | 55 ✓ | 53 ✓ | 59 ✓ |
| de | 57 ✓ | 58 ✓ | 53 ✓ | 57 ✓ |
| id | 50 ✓ | 55 ✓ | 55 ✓ | 57 ✓ |
| es | 51 ✓ | 58 ✓ | 52 ✓ | 52 ✓ |
| bn | 53 ✓ | 53 ✓ | 50 ✓ | 57 ✓ |
| ms | 51 ✓ | 52 ✓ | 53 ✓ | 55 ✓ |

**القالب المختار**: المنتقي يختار الأطول الواقع في [50,60]؛ بعد ضبط القوالب صار الطبقة **`long`** هي المختارة في عامّة اللغات (والسلّم يضمن السقوط إلى medium/short إن طالت أيّ سنة/متغيّر مستقبلًا). أمثلة:
- AR hijri-ny: «كم باقي على رأس السنة الهجرية 1448؟ عدّ تنازليّ ليوم 1 محرّم» (60)
- EN ramadan: «How Long Until Ramadan 1448? Countdown to the Fasting Month» (59)
- FR eid-fitr: «Combien de jours avant l’Aïd el-Fitr 1448 ? Compte à rebours» (60)

## 5) جدول الـMeta — الطول (كلّها داخل [120,160] ✓)
| اللغة | hijri-ny | ramadan | eid-fitr | eid-adha |
|---|---|---|---|---|
| ar | 144 | 141 | 142 | 140 |
| en | 147 | 160 | 160 | 151 |
| fr | 138 | 156 | 155 | 149 |
| tr | 150 | 154 | 143 | 158 |
| ur | 144 | 148 | 145 | 156 |
| de | 131 | 131 | 150 | 143 |
| id | 131 | 145 | 157 | 139 |
| es | 128 | 148 | 150 | 143 |
| bn | 137 | 138 | 136 | 145 |
| ms | 124 | 147 | 160 | 158 |

## 6) H1 — لكل لغة (الأربع صفحات × 10 لغات)
- **العدد = 1 بالضبط** على كل صفحة×لغة، ووسمه `id="cd-h1-{occ}"` (مُثبَت في السموك: 40/40).
- نصّ الـH1 يبقى مُترجَمًا عبر `data-i18n` (لم تُحذَف السمة) ⇒ «How Long Until Hijri New Year?» / «كم باقي على رمضان؟» … إلخ.

## 7) عدد الكلمات — قبل/بعد
**قبل** (حالة التسريب، FAQ + الجدول فارغان في SSR): AR ≈ 278 · EN ≈ 344 · FR ≈ 362 · UR ≈ 372 · BN ≈ 289 (والبقيّة في النطاق ~280–370).
**بعد** (نشط + FAQ مُحقَن + 4 أقسام تعليميّة):
| اللغة | hijri-ny | ramadan | eid-fitr | eid-adha | الهدف |
|---|---|---|---|---|---|
| ar | **815** | **800** | **804** | **810** | 800–900 ✓ |
| en | 953 | 877 | 894 | 934 | 800+ ✓ |
| fr | 983 | 983 | 940 | 975 | 700+ ✓ |
| tr | 726 | 713 | 701 | 709 | 700+ ✓ |
| ur | 999 | 999 | 973 | 996 | 700+ ✓ |
| de | 893 | 835 | 833 | 873 | مماثل ✓ |
| id | 782 | 772 | 761 | 757 | مماثل ✓ |
| es | 977 | 980 | 953 | 981 | مماثل ✓ |
| bn | 763 | 729 | 731 | 721 | مماثل ✓ |
| ms | 804 | 781 | 746 | 741 | مماثل ✓ |

## 8) تسلسل H2/H3
- **H1 = 1** · **H2 = 9** (جدول السنوات + روابط مفيدة + FAQ + المعلومات + الاعتماد + **4 أقسام تعليميّة محقونة**) · **H3 = 3** (المواضيع الثلاثة داخل قسم المعلومات الموجود).
- بطاقات الروابط المفيدة `<a>` لا تُضخّم عدّ الـH2 (عنوان قسم واحد فقط).

## 9) الأقسام التعليميّة المضافة (4 لكل مناسبة، عمليّة/تقويميّة — لا تُكرِّر القسم الدينيّ)
1. **متى تبدأ المناسبة؟** (التاريخ الهجريّ + الرؤية/الحساب) — مخصّصة لكل مناسبة (بدء الصيام / هلال شوّال بعد رمضان / 10 ذي الحجّة بعد عرفة / 1 محرّم).
2. **لماذا يختلف الموعد بين الدول؟** (الرؤية مقابل الحساب الفلكيّ).
3. **كيف يعمل هذا العدّ؟** (المؤقّت + جدول السنوات + اللوحة القمريّة).
4. **المناسبة والتقويم القمريّ** (انزياح ~11 يومًا والدورة ~33 عامًا) — مع لمسة مخصّصة (ساعات الصيام / تخطيط العيد / ربط عرفة بالحجّ …).

## 10) FAQ + JSON-LD
- **10 أسئلة مرئيّة في SSR** على كل صفحة×لغة (من مفاتيح i18n الموجودة، بتعويض `{n}`/`{date}`/`{hyear}` خادِم-جانبًا) — لم تُؤلَّف من جديد بل أصبحت مرئيّة.
- **FAQPage JSON-LD واحد** لكل صفحة، صالح JSON، `mainEntity.length = 10`، ونصّ كل سؤال/جواب **مطابق للمرئيّ** (مُثبَت في السموك على 40/40). لا تكرار FAQPage (كان 0 سابقًا).
- العميل يعيد ملء `#*-faq-list` بنفس مصدر i18n عبر `innerHTML=` ⇒ لا ازدواج ولا تعارض SSR↔hydration.

## 11) اختبار سلّم العنوان للأسماء الطويلة ✓
أربع وحدات اختبار في السموك تُثبت المنطق دون اعتماد على بيانات حقيقيّة:
- `long>60` ⇒ يختار medium الواقع في المدى. · `الكلّ>60` ⇒ يختار الأقصر (**بلا قصّ**). · `long في المدى` ⇒ يختاره (الأوصف). · `لا أحد في [50,60] وlong≤60` ⇒ يختار الأطول ≤60.
قابل للتوسّع: متغيّر مدينة/منطقة مستقبليّ يُضاف كطبقة أطول في `data/countdown-seo.js` دون تغيير المحرّك.

## 12) الملفّات المعدَّلة
| الملفّ | التغيير |
|---|---|
| `server.js` | **+211/−8** — `_COUNTDOWN_EVENTS` + `_nextCountdownTarget` + `_CD_GMONTHS`/`_cdGregDateStr` + `_CD_SEO_DATA`(require) + `_cdSubst`/`_pickCountdownTitle`/`_injectCountdownEdu` + فرع العنوان/الميتا في `buildSeoForPath` + كتلة SSR (تفعيل + FAQ + JSON-LD + حقن الأقسام) + تحويل 4 علامات H1 إلى `kind:'id'` |
| `index.html` | **+20/−8** — `id="cd-h1-{occ}"` على 4 وسوم H1 + 4 عناصر نائبة `<div id="cd-edu-{occ}">` |
| `data/countdown-seo.js` | **جديد** — محتوى SEO خادِم-فقط: 4 مناسبات × 10 لغات (titles/desc + 4 أقسام)، يُحمَّل بـrequire عند الإقلاع، **لا يُرسَل للعميل** |
| `scripts/_smoke_hijri_new_year_countdown_seo_content_h1_fix_1.mjs` | **جديد** — 424 تأكيدًا |

**لم يُمَسّ:** `css/style.css` · `js/app.js` · `sw.js` · `js/i18n*` · curated · sitemap · الحسابات · مسارات الخادم الأخرى.

## 13) CSS / SW / cache-buster
- **CSS: بلا تغيير** — الأقسام التعليميّة تعيد استخدام `.section-card .countdown-info` الموجودة؛ الـFAQ يعيد استخدام `.countdown-faq-list details`.
- **sw.js / cache-buster: بلا تغيير** — التعديل SSR-only، وHTML يُخدَم network-first (كتذاكر noindex/navbar)، والعميل (`app.js`) غير معدَّل ⇒ لا حاجة لرفع نسخة.

## 14) العدّاد/الحسابات/الجدول لم تتأثّر ✓
- الحاويات العميلة الأربع لكل صفحة (`#*-timer` · `#*-years-tbody` · `#*-faq-list`) **محفوظة** (مُثبَت: 12/12)، ومنطق `_initCountdown` في `app.js` غير ممسوس ⇒ المؤقّت الحيّ وجدول السنوات وأكورديون الـFAQ تعمل كما هي.
- التفعيل في SSR يطابق ما يفعله العميل عند الإقلاع ⇒ لا وميض ولا تعارض.

## 15) الصفحات الأخرى غير متأثّرة ✓
- 8 صفحات رئيسة (`/` · zakat · date-converter · azkar · msbaha · qibla · moon-today · today-hijri-date) ما زالت **H1=1 و200** (مُثبَت).
- **regression:** navbar **59/59** · noindex **39/39** (خطّ SSR العريض سليم). الفروع الجديدة محروسة بـ`_COUNTDOWN_EVENTS` فلا تُطلَق إلا على المسارات الأربعة.

## 16) النتائج
- **سموك التذكرة: 424/424 ✓** (40 صفحة×لغة × 10 فحوص + 12 حاوية + 8 صفحات أخرى + 4 وحدات سلّم العنوان).
- **regression:** navbar 59/59 · noindex 39/39.
- **`node --check`:** `server.js` ✓ · `data/countdown-seo.js` ✓ · السموك ✓.
- **OG/canonical/robots:** `og:type=website` · `robots=index,follow` · canonical صحيح لكل لغة · `og:title` يحمل العنوان الجديد (مُتحقَّق).

## 17) رسالة commit المقترحة
```
fix(seo): HIJRI-NEW-YEAR-COUNTDOWN-SEO-CONTENT-H1-FIX-1 — add SSR H1 and multilingual content depth with title fallbacks
```
الالتزام = `server.js` + `index.html` + `data/countdown-seo.js` + السموك + هذا التقرير (5 عناصر، معزولة).

---

**الخلاصة:** الصفحات الأربع صارت كاملة الـSEO في SSR بكل اللغات العشر: **عنوان ديناميكيّ ذكيّ (10 لغات داخل [50,60]) + Meta (داخل [120,160]) + H1 واحد فعليّ + 4 أقسام تعليميّة مخصّصة لكل مناسبة + 10 FAQ مرئيّة + FAQPage JSON-LD مطابق**، مع كلمات AR 800–815 وEN 877–953 وFR/TR/UR 700+. **بلا حشو، بلا تغيير تصميم/CSS/SW، بلا كسر للعدّاد أو الحسابات، بلا أثر على الصفحات الأخرى.** 424/424 + 59/59 + 39/39، node --check سليم.

**للاعتماد أرسِل:** `أعتمد دفع تقرير: HIJRI-NEW-YEAR-COUNTDOWN-SEO-CONTENT-H1-FIX-1` ثمّ «أوافق على تنفيذ الدفع».

*(ملاحظة ثابتة: لا أبدأ أيّ تذكرة جديدة ولا صفحة أذكار قبل اعتمادك؛ والأذكار موقوفة حتى اعتماد `/azkar/prayer-azkar` بصريًّا.)*
