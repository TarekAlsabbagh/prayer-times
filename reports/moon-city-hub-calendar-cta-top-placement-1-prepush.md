# MOON-CITY-HUB-CALENDAR-CTA-TOP-PLACEMENT-1 — PRE-PUSH REPORT

**التاريخ:** 2026-06-21 · **الحالة:** محليّ فقط — **لا commit، لا push** · بانتظار اعتمادك.
**HEAD الحاليّ:** `0beb5ec` (تصحيح الهب، مُغلق رسميًّا). التغيير الوحيد في شجرة العمل = أدناه.

---

## 0. الهدف (كما اعتمدته)

نقل بطاقة التقويم المختصرة `#moon-hub-cal` إلى **بداية محتوى صفحة الهب** المتداخلة `/moon/{country}/{city}` —
لتكون أوّل بطاقة محتوى واضحة يراها الزائر، **قبل** بطاقة طور القمر (`#moon-main-card`) وباقي أقسام القمر. هذا
**استثناء بصريّ/ترتيبيّ معتمد** من شرط legacy parity، بطلبك المباشر. لا تغيير نصّ، لا تصميم، لا تغيير للرابط
الديناميكيّ، لا grid كاملة في الهب، لا تغيير لصفحة الشهر، لا تغيير `js/moon.js`/Meeus 49.

> **Placement of #moon-hub-cal is intentionally changed by user request to appear at the beginning of the nested
> city hub page. This is the only approved visual/order deviation from the legacy hub parity requirement.**

---

## 1. التغيير (نقطة حقن واحدة — server.js فقط)

`server.js` — كتلة حقن البطاقة المختصرة (الفرع الخاصّ بالهب فقط، داخل `} else {` لـ `_isMonthPage`):

```js
// قبل: تُحقن قبل #moon-current-month-h2 (أسفل، بعد بطاقة طور القمر) — موضع legacy
html = html.replace(/(<h2 id="moon-current-month-h2")/, _hubCalCompactHtml + '\n                $1');
// بعد: تُحقن قبل #moon-main-card (أعلى المحتوى، قبل بطاقة طور القمر)
html = html.replace(/(<div[^>]*\sid="moon-main-card")/, _hubCalCompactHtml + '\n                $1');
```

تغيير الـ anchor فقط — **محتوى البطاقة (`_hubCalCompactHtml`) لم يتغيّر إطلاقًا** (نفس النصّ، نفس الرابط الديناميكيّ،
نفس التصميم والـ classes). الفرع محصور بالهب (الشهر يُحقن الشبكة الكاملة في مكان آخر، غير متأثّر).

**مجموعة الـ diff المتتبَّعة:** `server.js` + `scripts/_smoke_moon_city_hub_route_structure_add_1.mjs` (تأكيد الموضع)
— ملفّان فقط، **0 ضوضاء**. **لا cache-buster** (تغيير خادميّ بحت، لا أصل عميل تغيّر).

---

## 2. الموضع الجديد — مؤكَّد بالـ SSR والمتصفّح

ترتيب أعلى المحتوى المرئيّ على الهب (متصفّح، بعد hydration):
`… الهيدر/المقدّمة (hero) → breadcrumb → H1 → moon-city-answer → **#moon-hub-cal** → #moon-main-card → …`

| الفحص | النتيجة |
|---|---|
| `hero` (المقدّمة) قبل البطاقة | **نعم** ✅ |
| `breadcrumb` قبل البطاقة | **نعم** ✅ |
| H1 المرئيّ (`#moon-page-h1`) قبل البطاقة | **نعم** ✅ |
| البطاقة **قبل** `#moon-main-card` (بطاقة طور القمر) | **نعم** ✅ |
| البطاقة داخل `#page-moon` (ليست قبل الهيدر العامّ/navbar) | **نعم** ✅ |
| البطاقة **ليست داخل** الشريط اللاصق `#moon-sticky-bar` | **نعم** ✅ |
| عدد `#moon-hub-cal` (لا تكرار) | **1** ✅ |
| SSR: `cal(151034) < main(152743) < h2(162333)` | **مرتَّب صحيح** ✅ |

يطابق توجيهك حرفيًّا: بعد عناصر التعريف (hero/H1/breadcrumb)، قبل بطاقة طور القمر وكلّ الأقسام، لا قبل
الهيدر/navbar، لا داخل الشريط اللاصق.

---

## 3. الثوابت (لم تتغيّر بهذا التصحيح)

| العنصر | الحالة |
|---|---|
| نصّ البطاقة / تصميمها / classes | **بلا تغيير** ✅ |
| رابط البطاقة (متداخل + ديناميكيّ) | **بلا تغيير** — `/moon/saudi-arabia/riyadh/2026/06` (الشهر الحاليّ، لا hardcode، لا legacy، لا dash) ✅ |
| شبكة الشهر الكاملة `.moon-hub-cal-grid` في الهب | **غير موجودة** ✅ |
| `#moon-forecast` | **موجود** ✅ |
| صفحة الشهر `/moon/saudi-arabia/riyadh/2026/06` | **غير متأثّرة** — الشبكة الكاملة موجودة، بلا بطاقة مختصرة، 200 ✅ |
| `js/moon.js` | **لم يتغيّر** (`git diff` فارغ) ✅ |
| Meeus 49 | **لم يتغيّر** — 15 يونيو = المحاق، 30 يونيو = البدر ✅ |
| اليوم/today/السنة/الدولة | بلا تقويم/بلا تأثّر ✅ |

---

## 4. الاختبارات (كلّها خضراء)

**القمر:** hub **51/51** (+3 فحص موضع: البطاقة قبل `#moon-main-card`) · guardrails **118** · month **78** · day **62** ·
today **53** · year **74** · country **58** · phase **211** · meeus49 **45** · today-move **35** · spa **38**.
**غير القمر (انحدار):** search-place **659** · search-ar **22** · home-migration **33** · moon-box **47** · qibla-box **36** ·
qibla-back **12** · hijri ✅ · countdown **424**. **`node --check`** نظيف.

## 5. المتصفّح

`/moon/saudi-arabia/riyadh`: `#page-moon` نشط · البطاقة **ظاهرة** في أعلى المحتوى قبل بطاقة طور القمر ·
الرابط `/moon/saudi-arabia/riyadh/2026/06` · **لا شبكة كاملة** · `#moon-forecast` ظاهر · H1 = 1 ·
**لا تجاوز أفقيّ** · **0 أخطاء console**.

---

## 6. الحالة

التصحيح جاهز محليًّا ومُتحقَّق. بطاقة `#moon-hub-cal` انتقلت إلى **بداية محتوى صفحة الهب** (قبل بطاقة طور القمر،
بعد المقدّمة/H1/breadcrumb)، نسخة واحدة بلا تكرار، الرابط والتصميم والنصّ بلا تغيير، لا شبكة كاملة في الهب،
`#moon-forecast` محفوظ، صفحة الشهر بلا تأثّر، `js/moon.js` + Meeus 49 ثابتان. النطاق محصور بنقل الموضع فقط.
**لم أعمل commit ولم أدفع. بانتظار اعتمادك للتقرير ثمّ موافقتك الصريحة على الدفع.**
