# MOON-CITY-HUB-ROUTE-STRUCTURE-SCOPE-CORRECTION-FIX-1 — PRE-PUSH REPORT

**التاريخ:** 2026-06-21 · **الحالة:** محليّ فقط — **لا commit، لا push** · بانتظار اعتمادك.
**HEAD الحاليّ:** `ad07388` (تصحيح الشهر، منشور). التغيير الوحيد في شجرة العمل = أدناه.

---

## 0. الهدف (كما اعتمدته)

استعادة **بطاقة التقويم المختصرة `#moon-hub-cal`** على صفحة الهب المتداخلة `/moon/{country}/{city}` —
تمامًا كما كانت في الهب القديم `/moon-in-{city}` — مع **رابط متداخل ديناميكيّ** (الشهر الحاليّ، ليس ثابتًا)،
بصيغة `/moon/{country}/{city}/{yyyy}/{mm}`. **بلا** شبكة شهر كاملة على الهب، **بلا** حذف `#moon-forecast`،
**بلا** تغيير صفحة الشهر، **بلا** تصميم/نصّ/أقسام جديدة.

أصل المشكلة: تصحيح الشهر السابق (`ad07388`) ضيّق بوّابة كتلة التقويم من `_isMoonHubPageSsr` إلى
`_isMoonMonthPageSsr` بناءً على اعتقاد خاطئ بأنّ الهب القديم لم يكن يعرض أيّ تقويم. أثبت **HUB-LEGACY-PARITY-AUDIT-1**
أنّ الهب القديم **كان** يعرض البطاقة المختصرة. هذا التصحيح يُعيد البوّابة إلى `_isMoonHubPageSsr` مع إبقاء الشبكة
الكاملة للشهر فقط.

---

## 1. التغيير (سطر واحد + تعليق — server.js فقط)

`server.js:23103` — بوّابة كتلة التقويم:

```js
// قبل (تصحيح الشهر ad07388):
if (_isMoonMonthPageSsr && MoonCalc && typeof MoonCalc.getPhaseName === 'function') {
// بعد (هذا التصحيح):
if (_isMoonHubPageSsr && MoonCalc && typeof MoonCalc.getPhaseName === 'function') {
```

`_isMoonHubPageSsr = !!seo.moonCity.isHub` حيث `isHub = (_isMoonHubPage || _isMoonMonthPage)` — صحيحة على
الهب **و** الشهر، خاطئة على اليوم/today/السنة. التقسيم الداخليّ القائم `if (_isMonthPage) { شبكة كاملة } else { بطاقة مختصرة }`
يُبقي الشبكة للشهر حصرًا والبطاقة المختصرة للهب حصرًا. لا تغيير في أيّ منطق آخر، لا في صفحة الشهر، لا في الأصول العميلة.

**ملفّات أخرى = اختبارات فقط** (لا أصول عميلة، لا cache-buster — التغيير خادميّ بحت):
- `scripts/_smoke_moon_city_hub_route_structure_add_1.mjs` — قسم C2 جديد: البطاقة المختصرة + الرابط المتداخل الديناميكيّ + لا شبكة كاملة.
- `scripts/_smoke_moon_routes_structure_guardrails_1.mjs` — ثابِت الهب صار: «بطاقة مختصرة موجودة + لا شبكة كاملة».
- `scripts/_smoke_moon_city_month_route_structure_add_1.mjs` — قسم C2: الهب يعرض البطاقة المختصرة لا الشبكة الكاملة.

مجموعة الـ diff المتتبَّعة (منذ `ad07388`): `server.js` + 3 ملفّات smoke فقط — **0 ضوضاء**.

---

## 2. قيمة رابط البطاقة المختصرة — وما مصدره

| السؤال | الجواب |
|---|---|
| **ما قيمة `href`؟** | `/moon/saudi-arabia/riyadh/2026/06` (الرياض، الشهر الحاليّ يونيو 2026) |
| **من أين السنة والشهر؟** | `_calY = _calTodayD.getFullYear()` · `_calMo = _calTodayD.getMonth()+1` (server.js:23202-23207) |
| **ما `_calTodayD`؟** | `new Date()` عند 12:00 (server.js:23152) — **نفس «اليوم» الذي يستخدمه legacy** |
| **هل ثابت في الكود؟** | **لا.** لا `2026` ولا `06` مكتوبَين. مشتقّان من تاريخ اليوم حصرًا. |
| **كيف يُبنى الرابط؟** | `_nestedMoonMonthLink(seo.moonCity.slug, _langPrefixHc, _calY, _calMo)` (server.js:23677) → صيغة متداخلة `/moon/{country}/{city}/{yyyy}/{mm}` |

اقتباس الكود (المسار النشط على الهب — لا `?cal` ولا شهر في الـURL، فـ`_calM` = null):

```js
const _calY  = _calM ? … : _calTodayD.getFullYear();    // 23202-23204
const _calMo = _calM ? … : (_calTodayD.getMonth() + 1); // 23205-23207
const _hubCalCompactHref = _nestedMoonMonthLink(seo.moonCity.slug, _langPrefixHc, _calY, _calMo); // 23677
```

---

## 3. الإجابات الصريحة على أسئلتك

| السؤال | الجواب |
|---|---|
| **هل الرابط متداخل وصحيح؟** | نعم — `/moon/{country}/{city}/{yyyy}/{mm}` (شُرَط مائلة، عبر `_nestedMoonMonthLink`)، لا dash، لا legacy. |
| **هل يتغيّر بتغيّر الشهر/السنة؟** | نعم — ديناميكيّ بالبناء. الشهر القادم سيُصبح `/2026/07` تلقائيًّا؛ مطلع 2027 سيُصبح `/2027/01`. لا قيمة ثابتة. |
| **هل اللغة محفوظة؟** | نعم — `/en/…` يعطي `/en/moon/saudi-arabia/riyadh/2026/06` عبر `_langPrefixHc`. |
| **هل `/moon-in-riyadh` ما زال 301؟** | نعم — **301** → `/moon/saudi-arabia/riyadh` (بلا تغيير). |
| **هل الشهر الهدف 200؟** | نعم — `/moon/saudi-arabia/riyadh/2026/06` = **200**. |
| **روابط `#moon-chart-container`؟** | **تُركت legacy-fallback (Option-1)** — التفصيل في §6. |

---

## 4. اختبار الديناميكيّة (أكثر من حالة)

| الحالة | الرابط المُلتقَط من SSR | صحيح؟ |
|---|---|---|
| الشهر الحاليّ (الرياض، ar) | `/moon/saudi-arabia/riyadh/2026/06` | ✅ شهر اليوم |
| لغة مختلفة `/en` | `/en/moon/saudi-arabia/riyadh/2026/06` | ✅ بادئة اللغة محفوظة |
| مدينة أخرى — jeddah | `/moon/saudi-arabia/jeddah/2026/06` | ✅ قاعدة المدينة صحيحة |
| مدينة أخرى — mecca | `/moon/saudi-arabia/mecca/2026/06` | ✅ قاعدة المدينة صحيحة |

**عبور السنة:** مؤكَّد بالبناء — `_calTodayD.getFullYear()`/`getMonth()` يعكسان تاريخ النظام؛ لا تثبيت.
في يونيو 2026 يُنتجان `2026/06`؛ في يناير 2027 سيُنتجان `2027/01` بلا أيّ تغيير في الكود.
(تأكيد القيمة الثابتة `06` على المدن الأربع يُثبت القراءة من اليوم لا من قيمة مكتوبة.)

---

## 5. مطابقة الهب الجديد للهب القديم (worktree `64562f9` — آخر commit كان فيه `/moon-in-riyadh` = 200)

| المقياس | القديم (64562f9) | الجديد (بعد التصحيح) | النتيجة |
|---|---|---|---|
| عدد عناصر `id` | 108 | 108 | **متطابق ✅** |
| عدد `section-card` | 29 | 29 | **متطابق ✅** |
| عناصر زائدة في الجديد | — | لا شيء | **✅** |
| عناصر ناقصة في الجديد | — | لا شيء | **✅** |
| `#moon-hub-cal` (بطاقة مختصرة) | موجود | موجود | **مُستعاد ✅** |
| `#moon-forecast` | موجود | موجود | **محفوظ ✅** |

**قبل هذا التصحيح** كان الهب المتداخل 107 عنصرًا (تنقصه البطاقة المختصرة)؛ **بعده** 108 = مطابقة تامّة للقديم.
التغيير الجراحيّ الوحيد = استعادة عنصر `#moon-hub-cal` (107→108).

**ملاحظة أمانة عن النصّ المرئيّ:** النصّ المرئيّ ليس متطابقًا بايت-ببايت مع لقطة `64562f9` (فرق ~241 حرفًا). السبب
**تطوّر المحتوى/النسخة**: `64562f9` هو **الوحيد** الذي كان فيه `/moon-in-riyadh` يُعيد 200 (صار 301 في الـcommit التالي MCHRS)،
فنسخة محتوى `/moon` أُعيدت صياغتها/وُسِّعت عبر ~30 تذكرة منذ تلك اللقطة. الفرق هو **نصّ مختلف داخل نفس القسم** (لا قسم
زائد/ناقص — مجموعة العناصر متطابقة 108/108). وتصحيحنا تغيير بوّابة بسطر واحد لا يمكنه تعديل أيّ نصّ. النصوص الحاليّة على
الهب المتداخل تطابق نسخة `/moon` الإنتاجيّة الحاليّة (الصحيحة)، لا لقطة `64562f9` القديمة.

---

## 6. قرار روابط `#moon-chart-container` — تُركت legacy-fallback (Option-1)

روابط نقاط المخطّط (`#moon-chart-container`) ما زالت بصيغة legacy `/moon-in-{slug}/{iso}` (تُحلّ 301 إلى المتداخل).
**القرار: تركها كما هي ضمن هذا التصحيح**، للأسباب:

1. **ليست فرق-مطابقة** — الهب القديم `64562f9` كان يحملها بنفس صيغة legacy تمامًا؛ تغييرها يُبعِد الجديد عن القديم لا يُقرّبه.
2. **معالَجة 301** — عميليّة، تُحلّ تلقائيًّا إلى المتداخل (سياسة MLRC Option-1 المعتمدة)، فلا روابط ميّتة.
3. **نظام فرعيّ مختلف** — التحويل يتطلّب تمرير سياق الدولة إلى مُصيِّر المخطّط في `js/app.js`، وأنت نبّهت صراحةً «لا تكسر المخطّط».
4. **تذكرة-روابط-عميلة منفصلة أنسب** — يطال كلّ صفحات `#page-moon` لا الهب وحده؛ خارج نطاق إصلاح الهب المركَّز هذا.

هذا التصحيح **خادميّ بحت** ولا يلمس `js/app.js` إطلاقًا. (روابط الجدول `#moon-forecast` كانت قد حُوِّلت للمتداخل في MLRC.)

---

## 7. الاختبارات (كلّها خضراء)

**القمر:** hub **48/48** (+15 فحص بطاقة مختصرة) · guardrails **118/118** · month **78/78** · day **62** · today **53** ·
year **74** · country **58** · phase **211** · meeus49 **45** · today-move **35** · spa **38**.
**غير القمر (انحدار):** search-place **659** · search-ar **22** · home-migration **33** · moon-box **47** · qibla-box **36** ·
qibla-back **12** · hijri ✅ · countdown **424**. **`node --check`** نظيف على `server.js` + ملفّات smoke المُعدَّلة.

---

## 8. التحقّق في المتصفّح (الهب المُصحَّح)

`/moon/saudi-arabia/riyadh`: `#page-moon` نشط · البطاقة المختصرة **ظاهرة** (`offsetParent != null`) ·
`href=/moon/saudi-arabia/riyadh/2026/06` · `class="section-card moon-hub-calendar-card moon-hub-cal-compact"` ·
**لا** `.moon-hub-cal-grid` · `#moon-forecast` ظاهر · H1 = 1 · **لا تجاوز أفقيّ** · **0 أخطاء console**.

---

## 9. الثوابت المؤكَّدة

- **`js/moon.js` لم يتغيّر** — `git diff` فارغ.
- **Meeus 49 لم يتغيّر** — 15 يونيو 2026 = المحاق · 30 يونيو 2026 = البدر.
- **صفحة الشهر لم تتغيّر** — `/…/2026/06` ما زالت تعرض الشبكة الكاملة (`grid=true`، `compact=false`، page-moon).
- **اليوم/today/السنة بلا تقويم** — `isHub=false` عليها.
- **لا تصميم/نصّ/أقسام جديدة** — تغيير بوّابة فقط.
- **لا cache-buster** — لا أصل عميل تغيّر (server.js فقط).
- **`#moon-forecast` لم يُحذف** (محلول في HUB-LEGACY-PARITY-AUDIT-1: موجود في القديم والجديد).

---

## 10. الحالة

التصحيح جاهز محليًّا ومُتحقَّق. الهب المتداخل يعرض البطاقة المختصرة (`#moon-hub-cal`) برابط متداخل ديناميكيّ للشهر
الحاليّ (لغة محفوظة، لكلّ مدينة)، **بلا** شبكة كاملة، `#moon-forecast` محفوظ، مطابقة بنيويّة للقديم 108/108،
صفحة الشهر بلا تغيير، `js/moon.js` + Meeus 49 ثابتان، روابط المخطّط legacy-fallback (Option-1) مُوثَّقة.
**لم أعمل commit ولم أدفع. بانتظار اعتمادك للتقرير ثمّ موافقتك على الدفع.**
