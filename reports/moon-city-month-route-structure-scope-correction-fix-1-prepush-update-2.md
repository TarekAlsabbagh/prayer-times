# MOON-CITY-MONTH-ROUTE-STRUCTURE-SCOPE-CORRECTION-FIX-1 PRE-PUSH UPDATE 2

**التاريخ:** 2026-06-21 · **الفرع:** main · **الحالة:** مُنفَّذ محليًا فقط — **بلا commit، بلا push** · بانتظار اعتمادك.

> هذا التحديث يجيب على طلبك: صفحة الشهر المتداخلة يجب أن تكون **نفس** صفحة الشهر القديمة `/moon-in-{city}/{yyyy-mm}` تمامًا — محتوى وتصميم وترتيب ووظائف — لا زيادة ولا نقص. أجريتُ **مقارنة بايت-ببايت مع الصفحة القديمة الحقيقيّة** (لا مع إعادة بناء).

---

## 0. كيف قارنتُ (منهجيّة صارمة، لا تخمين)

بما أنّ `/moon-in-riyadh/2026-06` صار 301 بعد cleanup، لم أقارن من الرابط العام. بدلًا من ذلك:

- أنشأتُ **git worktree نظيفًا عند الـ commit `c5642d0`** (وهو الأب المباشر لِـ MLLRC، حيث كان `/moon-in-riyadh/2026-06` لا يزال **200** بمُصيِّره القديم الحقيقيّ).
- شغّلتُ خادمَين متوازيَين: القديم (8090) من ذلك الـ commit، والحاليّ (8091) بتعديلاتي.
- التقطتُ `#page-moon` من الصفحتين وقارنتُهما بنيويًّا (IDs + section-cards) ونصّيًّا (النصّ المرئيّ بعد تجريد الوسوم).
- ثم حذفتُ الـ worktree.

---

## 1. هل أصبحت صفحة الشهر الجديدة مطابقة للقديمة في المحتوى والتصميم والوظيفة؟

**نعم — مطابقة تمامًا.** نتائج المقارنة المباشرة بين القديمة (8090) والمتداخلة (8091):

| المقياس | القديمة `/moon-in-riyadh/2026-06` | المتداخلة `/moon/saudi-arabia/riyadh/2026/06` | النتيجة |
|---|---|---|---|
| عدد عناصر `id=` داخل `#page-moon` | 104 | 104 | **متطابق** |
| IDs ناقصة في المتداخلة | — | — | **[] (صفر)** |
| IDs زائدة في المتداخلة | — | — | **[] (صفر)** |
| عدد `section-card` | 34 | 34 | **متطابق** |
| section-cards ناقصة/زائدة | — | — | **[] / [] (صفر)** |
| النصّ المرئيّ (بعد تجريد breadcrumb) | 13954 حرفًا | 13954 حرفًا | **متطابق بايت-ببايت (IDENTICAL = true)** |

أي: نفس الأقسام، نفس ترتيب العناصر، نفس النصوص، نفس الكالندر، نفس الـ layout — لا زيادة ولا نقص.

---

## 2. ما الفروقات المتبقية؟ (يجب أن تكون فقط URL/canonical/hreflang/breadcrumb/روابط nested)

الفروقات الوحيدة المتبقية — وكلّها ضمن المسموح:

1. **URL** الجديد: `/moon/saudi-arabia/riyadh/2026/06`.
2. **canonical ذاتيّ** للرابط الجديد.
3. **hreflang** للرابط الجديد (10 لغات + x-default).
4. **Breadcrumb** بالبنية الجديدة: الرئيسية › حالة القمر › المملكة العربية السعودية › الرياض › 2026 › يونيو (DOM ≡ JSON-LD، 6 عناصر).
5. **روابط الأيّام** داخل الكالندر → `/moon/saudi-arabia/riyadh/2026/06/{dd}` (وخليّة «اليوم» → `…/today`).
6. **روابط تغيير الشهر** (السابق/التالي + نموذج المنتقي) → البنية الجديدة.
7. **رابط CTA «عرض حالة القمر اليوم»** → `…/today` المتداخل.

لا فرق آخر إطلاقًا (النصّ المرئيّ متطابق بايت-ببايت بعد استثناء الـ breadcrumb).

---

## 3. هل أزيل أي محتوى جديد كان زائدًا؟

**نعم.** القسم المُصمَّم السابق `#page-moon-month` (Hero/ملخّص/FAQ/جدول `my-day-link` المخصّص) **مُجرَّد بالكامل** من الـ HTML المُقدَّم. تأكيد: 0 آثار `my-chip`/`my-day-link`/`moon-month-hero`/`moon-month-summary`/`moon-month-calendar`/`my-month-card`. لا Hero جديد، لا ملخّص جديد، لا FAQ جديدة، لا جدول جديد، لا تصميم/أقسام SEO جديدة، لا محتوى زائد.

---

## 4. هل عاد نفس calendar/table القديم بالكامل؟

**نعم.** الكالندر المتداخل = نفس شبكة `.moon-hub-calendar-card` + `.moon-hub-cal-grid` القديمة (تقويم 7 أعمدة، Meeus 49)، نفس العنوان `📆 تقويم أطوار القمر في الرياض — يونيو 2026`، نفس عدد الخلايا، نفس أزرار السابق/التالي، نفس نموذج اختيار الشهر — بدليل أنّ النصّ المرئيّ مطابق بايت-ببايت. المتصفّح: الشبكة مرئيّة، **بلا console errors**، **بلا horizontal overflow**.

## 5. هل نفس layout القديم موجود؟

**نعم.** 104 عناصر `id` + 34 `section-card` متطابقة بالكامل وبنفس الترتيب → نفس الـ layout/spacing/البنية. لا فرق بصريّ.

---

## 6. هل تغيير الشهر لم يعد يذهب إلى 404؟

**نعم — كلّ روابط تغيير الشهر تعمل (200، بلا 404):**

| الإجراء | الوجهة | الحالة |
|---|---|---|
| السابق (من يونيو 2026) | `/moon/saudi-arabia/riyadh/2026/05` | **200** ✅ |
| التالي (من يونيو 2026) | `/moon/saudi-arabia/riyadh/2026/07` | **200** ✅ |
| السابق من يناير (跨 السنة) | `/moon/saudi-arabia/riyadh/2025/12` | **200** ✅ |
| التالي من ديسمبر (跨 السنة) | `/moon/saudi-arabia/riyadh/2027/01` | **200** ✅ |
| رابط يوم داخل الكالندر | `/moon/saudi-arabia/riyadh/2026/06/17` | **200** ✅ |

**اختبار نقر فعليّ في المتصفّح:** ضغطتُ زرّ «السابق» على يونيو 2026 → انتقلت الصفحة فعليًّا إلى `/moon/saudi-arabia/riyadh/2026/05` (ليست 404)، `page-moon` نشط، الشبكة مرئيّة، العنوان `📆 تقويم أطوار القمر في الرياض — مايو 2026`، وH1 `🌙 أطوار القمر في الرياض — مايو 2026`. أي زر/رابط داخل الكالندر لا يؤدّي إلى 404.

---

## 7. ما الروابط التي كانت خاطئة قبل الإصلاح؟ وما الصحيحة الآن؟

عند تشغيل كتلة التقويم لأوّل مرّة على صفحة الشهر (بعد إصلاح `_hubPath`)، انكشف **رابط legacy واحد** كانت كتلة التقويم تُصدِره ولم تُحوِّله MLRC (لأنّ الكتلة كانت تُرمى صامتةً فلم يُختبَر مخرَجها):

| المكان | قبل الإصلاح (خاطئ) | بعد الإصلاح (صحيح) |
|---|---|---|
| CTA «عرض حالة القمر اليوم» | `/moon-today-in-riyadh` (legacy) | `/moon/saudi-arabia/riyadh/today` (nested) ✅ |
| روابط أيّام الكالندر | (كانت تُرمى صامتةً) | `/moon/saudi-arabia/riyadh/2026/06/{dd}` ✅ |
| السابق/التالي + نموذج المنتقي | (كانت تُرمى صامتةً — `_hubPath`) | `/moon/saudi-arabia/riyadh/2026/05` و`/2026/07` + action نموذج المنتقي = `/moon/saudi-arabia/riyadh` ✅ |

**السبب الجذريّ للرابط الخاطئ:** شرط الـ CTA كان يفحص `seo.moonCity.isNested` (علم الـ **hub** المتداخل فقط) → على صفحة الشهر يساوي false فيقع على الـ fallback القديم. وسّعتُ الشرط ليقرأ سياق الشهر المتداخل (`isNestedMonth` + `nestedMonthCountrySlug`).

**التأكيد النهائيّ:** صفحة الشهر المتداخلة الآن **0 روابط SSR إلى `/moon-in-` أو `/moon-today-in-`** (تحقّق آليّ على الخادم الحاليّ).

---

## 8. نتائج اختبار السابق/التالي + يناير/ديسمبر跨 السنة

كلّها أعلاه في §6 — **200 في كلّ الحالات، بلا 404**، مع تأكيد نقر فعليّ في المتصفّح لِزرّ «السابق».

---

## 9. نتائج smoke / guardrails / regression (كلّها خضراء)

**اختبار الشهر** `_smoke_moon_city_month_route_structure_add_1.mjs` — **72/72** (أُضيف +3 فحوص: **0 روابط legacy** + CTA متداخل + prev/next/picker متداخل).

| Suite | النتيجة |
|---|---|
| `_smoke_moon_city_month_route_structure_add_1` (مُحدَّث) | ✅ 72/72 |
| `_smoke_moon_routes_structure_guardrails_1` (الشهر = `page-moon`+grid، الهب بلا تقويم) | ✅ 118/118 |
| `_smoke_moon_city_day_route_structure_add_1` | ✅ 62/62 |
| `_smoke_moon_city_hub_route_structure_add_1` | ✅ 33/33 |
| `_smoke_moon_city_today_route_structure_add_1` | ✅ 53/53 |
| `_smoke_moon_city_year_route_structure_add_1` | ✅ 74/74 |
| `_smoke_moon_country_pages_ssr_add_1` | ✅ 58/58 |
| `_smoke_moon_phase_calendar_calculation_fix_1` | ✅ 211/211 |
| `_smoke_moon_phase_event_engine_meeus49_fix_1` (carry) | ✅ 45/45 |
| `_smoke_moon_today_content_move_to_moon_1` | ✅ 35/35 |
| `_smoke_moon_spa_router_moon_prefix_activation_audit_1` | ✅ 38/38 |

**انحدار غير القمر:** search-place **659/659** · search-ar **22/22** · home-migration **33/33** · moon-box **47/47** · qibla-box **36/36** · qibla-back **12/12** · hijri **ALL PASSED** · countdown **424/424**. `node --check` نظيف على كلّ ملفّ مُعدَّل.

---

## 10. قائمة الملفات المعدّلة (9 ملفّات + تقريران — staging صريح بالمسار، 0 ضوضاء)

**الأساس (4):** `server.js` · `js/app.js` · `index.html` · `sw.js`
**العقد (1):** `reports/moon-routes-structure-contract-1.md`
**الاختبارات (4):** `scripts/_smoke_moon_city_month_route_structure_add_1.mjs` · `_smoke_moon_routes_structure_guardrails_1.mjs` · `_smoke_moon_city_day_route_structure_add_1.mjs` · `_smoke_moon_phase_calendar_calculation_fix_1.mjs`
**التقارير (2):** `reports/…-prepush-update.md` + `reports/…-prepush-update-2.md` (هذا).

**تعديلات `server.js` في هذه التذكرة (5 مواضع):** (1) `_pickerActionHref = _hubPath` → `_moonNestBaseHc` (إصلاح ReferenceError)؛ (2) بوّابة كتلة التقويم `_isMoonHubPageSsr` → `_isMoonMonthPageSsr` (التقويم لصفحة الشهر فقط)؛ (3) كتلة تعبئة DOM لِـ breadcrumb الشهر (6 مستويات)؛ (4) **CTA «عرض اليوم» → nested عبر `nestedMonthCountrySlug`** (هذا التحديث)؛ + تركيب `_MNESTED_MONTH` وتجريد القسم المخصّص من الجلسة السابقة. **Cache busters:** `app.js?v=795` · `sw v455` (بلا تغيير إضافيّ — التعديلات كلّها server-side).

---

## 11. تأكيد أنّ `js/moon.js` لم يتغيّر

`git diff --stat js/moon.js` = **فارغ**. لم يُلمَس.

## 12. تأكيد أنّ Meeus 49 لم يتغيّر

محرّك Meeus Ch.49 سليم؛ والنصّ المرئيّ للكالندر مطابق بايت-ببايت للقديم. تحقّق: **15 يونيو 2026 = المحاق** · **30 يونيو 2026 = البدر** (في الشبكة وفي صفحات اليوم المتداخلة).

---

## 13. لم أُغيّر / لم أبدأ

- **لم أُغيّر:** `js/moon.js`، Meeus 49، الحسابات، `/moon`، صفحة الدولة، **صفحة المدينة hub** (بلا تقويم، بلا تغيير)، صفحة السنة، صفحة اليوم، صفحة today، الصلاة/الهجري/القبلة/الأذكار/discovered.
- **لم أبدأ:** أيّ تذكرة جديدة، أيّ صفحة/تصميم/محتوى جديد.
- `/moon-in-riyadh/2026-06` يبقى **301 → نفس الرابط المتداخل**؛ والصيغ المسطّحة داخل nested (`…/2026-06`، `…/2026-06-17`) = **404**.

---

## 14. رسالة الـ commit المقترَحة + الحالة

```
fix(moon): MOON-CITY-MONTH-ROUTE-STRUCTURE-SCOPE-CORRECTION-FIX-1 — render new month routes with legacy calendar content
```

**مُنفَّذ محليًّا فقط. لا commit. لا push.** بانتظار مراجعتك واعتمادك.
