# Moon Routes — Structure Contract (MOON-ROUTES-STRUCTURE-GUARDRAILS-1)

**التاريخ:** 2026-06-17 (أُنشئ) · **آخر تحديث:** 2026-06-21 (`MOON-CITY-MONTH-ROUTE-STRUCTURE-SCOPE-CORRECTION-FIX-1` — صفحة الشهر `/moon/{country}/{city}/{yyyy}/{mm}` صارت **البديل البنيويّ** للصفحة القديمة `/moon-in-{city}/{yyyy-mm}`: تُعيد استعمال **نفس مُصيِّر `#page-moon` القديم** + **نفس شبكة التقويم الشهريّة القديمة** (`.moon-hub-cal-grid`، روابط الأيّام → اليوم المتداخل `/moon/{country}/{city}/{yyyy}/{mm}/{dd}`) — **لا** قسم `page-moon-month` مخصّص ولا Hero/FAQ/جدول جديد. الفروقات الوحيدة: canonical ذاتيّ + hreflang + breadcrumb 6 مستويات DOM≡JSON-LD + روابط أيّام التقويم متداخلة. **شبكة التقويم مُقيَّدة بصفحة الشهر فقط (`_isMoonMonthPageSsr`)** فالهب `/moon/{country}/{city}` يبقى بلا تقويم (لا grid ولا compact CTA). أصلح أيضًا خطأ `_hubPath is not defined` قديمًا كان يُسقِط كتلة التقويم صامتةً لكلّ صفحات القمر المتداخلة. سابقًا بنفس اليوم `MOON-LEGACY-ROUTES-CLEANUP-BEFORE-LAUNCH` — **كلّ المسارات المسطّحة القديمة للقمر صارت 301 → البنية المتداخلة** (مع حفظ اللغة): `/moon-today` → `/moon`، `/moon-in-{city}` → الهب المتداخل، `/moon-today-in-{city}` → `…/today`، `/moon-in-{city}/{yyyy-mm}` → `…/{yyyy}/{mm}`، `/moon-in-{city}/{yyyy-mm-dd}` → `…/{yyyy}/{mm}/{dd}`؛ تاريخ/شهر غير صالح + مدينة مجهولة بلا coord → **404**؛ coord-suffix يُحلّ لِصفحة 200؛ **كلّ المسطّحة خرجت من sitemap** (المتداخل فقط، بلا إغراق أيّام)؛ روابط SSR + canonical + breadcrumb كلّها متداخلة؛ روابط العميل (app.js) التي تملك دولة موثوقة (`_MOON_CITY_COUNTRY_KEYS` أو بذرة SSR أو `currentCountryCode` لِلمدينة الحاليّة أو `countryCode` نتيجة بحث) صارت متداخلة عبر `_nestedMoonHrefClient`؛ والتي بلا سياق دولة موثوق تبقى legacy-fallback تُحلّ عبر 301 (سياسة Option-1 المعتمَدة). سابقًا `MOON-CITY-TODAY-ROUTE-STRUCTURE-ADD-1` — فعّلت صفحة اليوم المتداخلة `/moon/{country}/{city}/today` كبديل بنيويّ لِلقديمة `/moon-today-in-{city}`: تُعيد استعمال **نفس مُصيِّر صفحة اليوم القديم** — `page-moon` نشط، نفس `#moon-page-h1`/`#moon-city-answer`/`<title>` — والفروقات الوحيدة: route + canonical ذاتيّ + hreflang + breadcrumb 5 مستويات DOM≡JSON-LD. روابط «اليوم» داخل الصفحات الجديدة تشير للمتداخل؛ القديم يبقى 200 بلا تحويل؛ today أُضيف لِـ sitemap بجانب القديم بلا تنظيف. **البنية المتداخلة الكاملة (`/moon` → `/{country}` → `/{city}` → `/today` + `/{yyyy}` → `/{mm}` → `/{dd}`) صارت مكتملة.** قبلها بِنفس اليوم `MOON-CITY-DAY-ROUTE-STRUCTURE-SCOPE-CORRECTION-FIX-1` — صفحة اليوم `/moon/{country}/{city}/{yyyy}/{mm}/{dd}` صارت **البديل البنيويّ** لِلصفحة القديمة `/moon-in-{city}/{yyyy-mm-dd}`: تُعيد استعمال **نفس مُصيِّر الصفحة المؤرّخة القديم** — `page-moon` نشط، نفس `#moon-page-h1`، نفس جسم `#moon-city-answer`، نفس `<title>` — **لا** قسم `page-moon-day` مخصّص ولا Hero/FAQ/جدول جديد. الفروقات الوحيدة المسموحة: (1) canonical ذاتيّ (الرابط المتداخل الجديد، حتّى خارج النطاق)، (2) hreflang للرابط المتداخل، (3) breadcrumb 7 مستويات (الرئيسية › حالة القمر › {Country} › {City} › {yyyy} › {Month} › {dd}) DOM≡JSON-LD. قبلها بِيوم كانت `MOON-CITY-DAY-ROUTE-STRUCTURE-ADD-1` قد فعّلتها كصفحة مخصّصة `page-moon-day` (نطاق خاطئ — أُلغي بِالتصحيح للأمام دون revert). والأعمق منها `/{dd}/…` و`today` وصور الشرطة dash يبقى 404 نظيف؛ روابط الأيّام في صفحة الشهر تشير إلى اليوم المتداخل الجديد؛ صفحات اليوم index + canonical ذاتيّ لكن **غير مُضافة دفعةً إلى sitemap**). · سابقًا 2026-06-18 (`MOON-CITY-MONTH-ROUTE-STRUCTURE-ADD-1` — فعّلت صفحة الشهر `/moon/{country}/{city}/{yyyy}/{mm}` في قسم `page-moon-month` مستقلّ). · سابقًا 2026-06-18 (`MOON-CITY-YEAR-ROUTE-STRUCTURE-ADD-1` — صفحة السنة `/moon/{country}/{city}/{yyyy}`). · سابقًا 2026-06-18 (`MOON-CITY-HUB-ROUTE-STRUCTURE-ADD-1` — `/moon/{country}/{city}` + 301 من `/moon-in-{city}`).
هذا الملفّ هو **مصدر الحقيقة** لِحالة روابط القمر الحاليّة والمستقبليّة. أيّ تذكرة لاحقة تُغيّر هذا العقد يجب أن تُحدّث هذا الملفّ + اختبار الحماية `scripts/_smoke_moon_routes_structure_guardrails_1.mjs` معًا.

---

## 1) الحالة الحاليّة المعتمَدة (LIVE — مثبَّتة بِالاختبار)

| الرابط | الحالة | canonical | sitemap | الصفحة النشطة | ملاحظة |
|---|---|---|---|---|---|
| `/moon` (+10 لغات) | **200** | ذاتيّ `…/moon` | **موجود** (index) | `page-moon` | هب «قمر اليوم» الرسميّ — نفس محتوى moon-today |
| `/moon/{country}` (+10 لغات، دول لها مدن curated) | **200** | ذاتيّ `…/moon/{country}` | **موجود** (index) | قالب المدن `prayer-times-cities.html` (variant=moon) | صفحة دولة — مراحل القمر، تعيد استعمال شبكة مدن الدولة؛ البطاقات والبحث → `/moon-today-in-{city}` (MOON-COUNTRY-PAGES-SSR-ADD-1) |
| `/moon-today` (+لغات، +/) | **301 → /moon** | — (لا جسم) | **غير موجود** | — | تحويلة دائمة، حفظ اللغة |
| `/moon/{country}/{city}` (+10 لغات) | **200** | ذاتيّ `…/moon/{country}/{city}` | **موجود** (cities sitemap) | `page-moon` | **هب المدينة الجديد** (البنية المتداخلة) — نفس محتوى `/moon-in-{city}` + breadcrumb 4 مستويات (MOON-CITY-HUB-ROUTE-STRUCTURE-ADD-1) |
| `/moon/{country}/{city}/today` (+10 لغات، المقطع الحرفيّ `today` فقط، lowercase) | **200** | ذاتيّ `…/moon/{country}/{city}/today` | **موجود** (cities sitemap، بجانب القديم — بلا تنظيف) | **`page-moon`** (نفس مُصيِّر صفحة اليوم القديمة) | **البديل البنيويّ لِصفحة اليوم القديمة `/moon-today-in-{city}`** — يُعيد استعمال **نفس** المُصيِّر والجسم: `page-moon` نشط، نفس `#moon-page-h1`، نفس `#moon-city-answer`، نفس `<title>`. **لا** قسم/Hero/FAQ/تصميم جديد. الفروقات الوحيدة: (1) route الجديد، (2) canonical ذاتيّ، (3) hreflang للرابط المتداخل، (4) breadcrumb 5 مستويات (الرئيسية › حالة القمر › {Country} › {City} (رابط) › اليوم) DOM≡JSON-LD. روابط «اليوم» داخل الصفحات الجديدة (هب المدينة: CTA + خليّة اليوم في التقويم) صارت تشير للمتداخل الجديد؛ القديم `/moon-today-in-{city}` يبقى 200 بلا تحويل (MOON-CITY-TODAY-ROUTE-STRUCTURE-ADD-1) |
| `/moon/{country}/{city}/{yyyy}` (+10 لغات، 1900–2100) | **200** | ذاتيّ `…/moon/{country}/{city}/{yyyy}` | **موجود** (cities sitemap — السنة السابقة + الحاليّة + التالية فقط) | **`page-moon-year`** (قسم مستقلّ) | **صفحة السنة** — breadcrumb 5 مستويات + جدول مراحل رئيسيّة (Meeus 49) + 12 بطاقة شهر (روابط → **الشهر المتداخل الجديد `/moon/{country}/{city}/{yyyy}/{mm}`** بعد MOON-CITY-MONTH-ROUTE-STRUCTURE-ADD-1) + ملخّص سنة + 6 FAQ + روابط سنة سابقة/تالية (MOON-CITY-YEAR-ROUTE-STRUCTURE-ADD-1) |
| `/moon/{country}/{city}/{yyyy}/{mm}` (+10 لغات، السنة 1900–2100، الشهر `\d{2}` في 01–12) | **200** | ذاتيّ `…/moon/{country}/{city}/{yyyy}/{mm}` | **موجود** (cities sitemap — أشهر السنة السابقة + الحاليّة + التالية، مربوطة بالسنوات المقبولة) | **`page-moon`** (المُصيِّر القديم — **لا** `page-moon-month`) | **صفحة الشهر = البديل البنيويّ للقديمة `/moon-in-{city}/{yyyy-mm}`** — نفس مُصيِّر `#page-moon` + **نفس شبكة التقويم الشهريّة القديمة `.moon-hub-cal-grid`** (Meeus 49، تقويم 7 أعمدة، روابط الأيّام → **اليوم المتداخل `/moon/{country}/{city}/{yyyy}/{mm}/{dd}`**، خليّة «اليوم» → `…/today`). الفروقات الوحيدة: canonical ذاتيّ + hreflang + breadcrumb 6 مستويات DOM≡JSON-LD (رتبة السنة رابط، رتبة الشهر current). **التقويم مُقيَّد بصفحة الشهر فقط** فالهب بلا تقويم (MOON-CITY-MONTH-ROUTE-STRUCTURE-SCOPE-CORRECTION-FIX-1) |
| `/moon/{country}/{city}/{yyyy}/{mm}/{dd}` (+10 لغات، السنة 1900–2100، الشهر 01–12، اليوم `\d{2}` صالح حسب الشهر leap-aware) | **200** | ذاتيّ `…/{yyyy}/{mm}/{dd}` (حتّى خارج النطاق) | **غير موجود** (index + canonical ذاتيّ، لكن **لا إضافة دفعة لِـ sitemap** — تُكتشَف عبر روابط أيّام صفحة الشهر) | **`page-moon`** (نفس مُصيِّر الصفحة المؤرّخة القديم) | **البديل البنيويّ لِلصفحة المؤرّخة القديمة `/moon-in-{city}/{yyyy-mm-dd}`** — يُعيد استعمال **نفس** المُصيِّر والجسم: `page-moon` نشط، نفس `#moon-page-h1`، نفس `#moon-city-answer`، نفس `<title>`. **لا** قسم/Hero/FAQ/جدول مخصّص. الفروقات الوحيدة: (1) canonical ذاتيّ (الرابط المتداخل، حتّى خارج النطاق = noindex لكن ذاتيّ — لا يُحوَّل للقديم `/moon-today-in-{city}`)، (2) hreflang للرابط المتداخل، (3) breadcrumb 7 مستويات (الرئيسية › حالة القمر › {Country} › {City} › {yyyy} › {Month} › {dd}، رتبتا السنة والشهر روابط) DOM≡JSON-LD (MOON-CITY-DAY-ROUTE-STRUCTURE-SCOPE-CORRECTION-FIX-1؛ سابقًا كانت `page-moon-day` مخصّصة — نطاق خاطئ أُلغي) |
| `/moon-in-{city}` (+coord) | **301 → /moon/{country}/{city}** | — (لا جسم) | **غير موجود** | — | الهب القديم — صار تحويلة دائمة للبنية المتداخلة (+حفظ اللغة) |
| `/moon-today-in-{city}` (+coord) | **301 → /moon/{country}/{city}/today** | — (لا جسم) | **غير موجود** | — | صفحة اليوم القديمة — صارت تحويلة دائمة للبنية المتداخلة (+حفظ اللغة)؛ مدينة مجهولة بلا coord → **404**؛ coord-suffix → تُجرَّد للصورة النظيفة ثمّ 301 (MOON-LEGACY-ROUTES-CLEANUP-BEFORE-LAUNCH) |
| `/moon-in-{city}/{YYYY-MM}` | **301 → /moon/{country}/{city}/{yyyy}/{mm}** | — (لا جسم) | **غير موجود** | — | صفحة الشهر القديمة — صارت 301 للبنية المتداخلة (+لغات)؛ شهر غير صالح (`2026-00`/`2026-13`) → **404** (MLRC) |
| `/moon-in-{city}/{YYYY-MM-DD}` | **301 → /moon/{country}/{city}/{yyyy}/{mm}/{dd}** | — (لا جسم) | **غير موجود** | — | صفحة اليوم القديمة — صارت 301 للبنية المتداخلة (+لغات)؛ تاريخ غير صالح (`2026-06-32`/`2026-02-30`) أو سنة هجريّة (`1447-…`) → **404** (MLRC) |

**ثوابت SEO:** `/moon` = index + canonical ذاتيّ + في sitemap. لا تكرار canonical بين `/moon` و`/moon-today` (الأخيرة 301 بلا جسم). صفحات المدن canonical ذاتيّ.

**ثوابت العميل (SPA):** كلّ ما سبق + بادئة `/moon/...` المستقبليّة تُصنَّف `page-moon` عبر `_isMoonPath` في `js/app.js`. صفحات غير القمر (`/`، `/prayer-times-in-*`، `/qibla-in-*`، `/today-hijri-date`، `/date-converter`) **ليست** `page-moon`.

**ثوابت Meeus 49 (`js/moon.js` — لا يُمَسّ):** الرياض يونيو 2026: 15=**المحاق** · 16=**هلال متزايد** · 29=**أحدب متزايد** (ليس بدرًا) · 30=**البدر**. صفحات المدن تستعمل IANA الخاصّ بالمدينة (`_hijriForIana`)، و`_CC_TO_PRIMARY_TZ` فقط fallback للمدن خارج الخريطة.

---

## 2) الحالة المستقبليّة (غير مفعّلة بعد — لا تُفعَّل إلا في تذاكر لاحقة مستقلّة)

| الرابط المستقبليّ | الحالة الحاليّة المطلوبة | عند التفعيل (تذكرة لاحقة) |
|---|---|---|
| `/moon/{country}/{city}/today/…` (أعمق من today) | **404 نظيف** | لا شيء أعمق من «today»؛ أمّا `…/today` نفسه فصار **200 LIVE** (`MOON-CITY-TODAY-ROUTE-STRUCTURE-ADD-1`) |
| `/moon/{country}/{city}/Today` أو `/TODAY` (أحرف كبيرة) | **404 نظيف** | المسارات lowercase فقط — `today` الحرفيّ الصغير فقط = 200 |
| `/moon/{country}/{city}/{yyyy}/{mm}/{dd}/…` (أعمق من اليوم) | **404 نظيف** | لا شيء أعمق من اليوم في البنية |
| `/moon/{country}/{city}/{YYYY-MM}` (صورة الشرطة dash) | **404 نظيف** | **ليست جزءًا من البنية الجديدة** — تبقى 404؛ صفحة الشهر القديمة هي `/moon-in-{city}/{YYYY-MM}` |
| `/moon/{country}/{city}/{YYYY-MM-DD}` (صورة الشرطة dash) | **404 نظيف** | **ليست جزءًا من البنية الجديدة** — تبقى 404؛ صفحة اليوم القديمة هي `/moon-in-{city}/{YYYY-MM-DD}` |

> ملاحظة: `/moon/{country}` (`MOON-COUNTRY-PAGES-SSR-ADD-1`)، و`/moon/{country}/{city}` (`MOON-CITY-HUB-…`)، و`/moon/{country}/{city}/today` (`MOON-CITY-TODAY-ROUTE-STRUCTURE-ADD-1`)، و`/moon/{country}/{city}/{yyyy}` (`MOON-CITY-YEAR-…`)، و`/moon/{country}/{city}/{yyyy}/{mm}` (`MOON-CITY-MONTH-…`)، و`/moon/{country}/{city}/{yyyy}/{mm}/{dd}` (`MOON-CITY-DAY-ROUTE-STRUCTURE-ADD-1`) كلّها صارت **200 LIVE** (انظر القسم 1) — البنية المتداخلة الكاملة مكتملة الآن. الجدول أعلاه يخصّ فقط ما تبقّى غير مُفعَّل: **الأعمق من today** + **الأعمق من اليوم** + **صور الشرطة dash** + **الأحرف الكبيرة `Today`**. **السنة 4 أرقام (`\d{4}`) في 1900–2100، الشهر رقمان (`\d{2}`) في 01–12، اليوم رقمان (`\d{2}`) صالح حسب أيّام الشهر (leap-aware)**؛ أيّ شيء آخر (`2026/6` / `2026/00` / `2026/13` / `2026/06/7` / `2026/06/00` / `2026/06/32` / `2026/02/30` / `2026/02/29` غير كبيسة / `2026-06` / `2026-06-17`) = 404.

**التحقّق من البنية المتداخلة (MOON-CITY-HUB-ROUTE-STRUCTURE-ADD-1):** `/moon/{country}/{city}` يُخدَم **200** فقط حين تكون الدولة حقيقيّة (`_countryFromSlug`) **و** المدينة تُحلّ (`_resolveCcForSlug`) **و** تنتمي لتلك الدولة (`makeCountrySlugSrv(cc)===country`). دولة/مدينة مجهولة → **404**. مدينة في دولة خاطئة → **301** إلى `/moon/{correctCountry}/{city}` (لا فهرسة لرابط غير مطابق).

**القاعدة الآن:** السيرفر يُرجِع **404** (صفحة خطأ صغيرة ~2KB، ليست shell 200KB) لِكلّ مسار متداخل **أعمق من الشهر** `/moon/{country}/{city}/{yyyy}/{mm}/{dd}` ولِـ`/moon/{country}/{city}/{today}` ولِصور الشرطة dash `/{YYYY-MM}` + `/{YYYY-MM-DD}` ولِلشهر غير الصالح (`/{yyyy}/6` أو `/{yyyy}/00` أو `/{yyyy}/13`). أمّا `/moon/{country}` و`/moon/{country}/{city}` و`/moon/{country}/{city}/{yyyy}` و`/moon/{country}/{city}/{yyyy}/{mm}` فتُرجِع **200**. العميل (`_isMoonPath`) يعرف كلّ بادئات `/moon/...` كـ`page-moon`؛ وعلى الهب المتداخل يُطبِّع العميل المسار (`_moonPathname`) إلى `/moon-in-{city}` لِكلّ مُحلِّلات القمر، ويترك breadcrumb الـ4 مستويات المحقون من SSR كما هو. روابط الهب المتداخل **داخل** cities sitemap؛ الهب القديم `/moon-in-{city}` **خرج** منها (لأنّه 301).

**ممنوع عند التفعيل لاحقًا:** إغراق sitemap بِملايين صفحات الأيّام (`…/{YYYY-MM-DD}`)؛ canonical خاطئ؛ صفحة index ناقصة؛ 200 فارغة.

---

## 3) ما الذي يحرسه اختبار `_smoke_moon_routes_structure_guardrails_1.mjs`
A) مُصنِّف `_isMoonPath` (مستخرَج فعليًّا) — كلّ القديم + الجديد `/moon/...` = page-moon؛ غير القمر = لا.
B) `/moon` = 200 + H1 واحد + page-moon + canonical ذاتيّ + index + محتوى فعليّ + FAQ + search hero + ليس footer-only.
C) `/moon-today` = 301→/moon (لغات +/)، ليست 200، خارج sitemap.
D) روابط مدينة القمر: الهب المتداخل `/moon/{country}/{city}` = 200 + page-moon + H1 واحد + canonical ذاتيّ + breadcrumb 4 مستويات؛ **كلّ المسطّحة القديمة صارت 301 → المتداخل** (+لغات، MOON-LEGACY-ROUTES-CLEANUP-BEFORE-LAUNCH): الهب `/moon-in-{city}` → الهب المتداخل، `/moon-today-in-{city}` → `…/today`، `/moon-in-{city}/{yyyy-mm}` → `…/{yyyy}/{mm}`، `/moon-in-{city}/{yyyy-mm-dd}` → `…/{yyyy}/{mm}/{dd}`؛ تاريخ/شهر غير صالح + مدينة مجهولة (بلا coord) → **404**؛ coord-suffix يبقى يُحلّ لِصفحة 200.
E) `/moon/{country}` = **200** صفحة دولة؛ الهب المتداخل `/moon/{country}/{city}` = **200**؛ **صفحة اليوم المتداخلة `…/today` = 200 + `page-moon` نشط (نفس محتوى الصفحة القديمة `/moon-today-in-{city}`: نفس `#moon-page-h1` + `<title>` + `#moon-city-answer`، **لا** قسم مخصّص) + H1 واحد + canonical ذاتيّ + breadcrumb 5 مستويات DOM≡JSON-LD**؛ **صفحة السنة `…/{yyyy}` = 200 + `page-moon-year` نشط**؛ **صفحة الشهر `…/{yyyy}/{mm}` = 200 + `page-moon` نشط (نفس محتوى الصفحة القديمة `/moon-in-{city}/{yyyy-mm}` + **شبكة التقويم الشهريّة `.moon-hub-cal-grid`** بروابط أيّام متداخلة، **لا** `page-moon-month`) + breadcrumb 6 مستويات DOM≡JSON-LD؛ والهب `/moon/{country}/{city}` يبقى بلا تقويم (لا grid/compact)**؛ **صفحة اليوم `…/{yyyy}/{mm}/{dd}` = 200 + `page-moon` نشط (نفس محتوى الصفحة المؤرّخة القديمة، **لا** `page-moon-day`) + breadcrumb 7 مستويات DOM≡JSON-LD (leap-aware: 2024/02/29=200)**؛ الأعمق/غير الصالح `…/{today/extra|Today|yyyy/6|yyyy/00|yyyy/13|yyyy/mm/7|yyyy/mm/00|yyyy/mm/32|yyyy/02/30|yyyy/02/29-غير-كبيسة|yyyy/mm/dd/extra}` **وصور الشرطة dash `/{YYYY-MM}` + `/{YYYY-MM-DD}`** = **404 نظيف**؛ دولة/مدينة مجهولة = **404**؛ مدينة في دولة خاطئة = **301** للصحيح.
F) sitemap-main: فيه `/moon` و`/moon/{country}`، ليس فيه `/moon-today`، لا إغراق أيّام. sitemap-cities: فيه الهب المتداخل `/moon/{country}/{city}` + **اليوم المتداخل `…/today`** + **صفحة السنة `…/{yyyy}` (سابقة+حاليّة+تالية)** + **صفحات الشهر `…/{yyyy}/{mm}` (12 شهرًا لِكلّ سنة مقبولة)**، و**خلت من كلّ المسطّحة القديمة** (`/moon-in-{city}` + `/moon-today-in-{city}` + المؤرّخة `/moon-in-{city}/{date}`) — كلّها 301 (MOON-LEGACY-ROUTES-CLEANUP-BEFORE-LAUNCH)، **ولا** صفحات يوم متداخلة `…/{yyyy}/{mm}/{dd}` (index + canonical ذاتيّ لكن غير مُضافة دفعةً — تُكتشَف عبر روابط أيّام صفحة الشهر).
+ اختبار مخصّص `scripts/_smoke_moon_city_hub_route_structure_add_1.mjs`: تطابق breadcrumb DOM≡JSON-LD (4 مستويات، AR+EN) + hreflang 10 لغات + x-default + بذرة `__PRAYER_CITY__` + 301 قديم→جديد (+لغات) + mismatch→301 + 404 التحقّق.
+ اختبار مخصّص `scripts/_smoke_moon_city_today_route_structure_add_1.mjs` (MOON-CITY-TODAY-ROUTE-STRUCTURE-ADD-1): اليوم المتداخل 200 + **`page-moon` نشط (لا `page-moon-day`)** + H1 واحد + canonical ذاتيّ للرابط المتداخل (4 مدن/لغات) + **نفس محتوى الصفحة القديمة `/moon-today-in-{city}`** (نفس `<title>` + `#moon-page-h1` + جسم `#moon-city-answer`، **0** عناصر مخصّصة) + breadcrumb 5 مستويات DOM≡JSON-LD (AR+EN، رتبة المدينة رابط، «اليوم» حاليّ) + hreflang 10+x-default للرابط المتداخل + التحقّق (today/test + Today أحرف-كبيرة + dash = 404، mismatch=301، دولة/مدينة مجهولة=404) + sitemap (اليوم المتداخل موجود + القديم باقٍ) + روابط «اليوم» في الهب صارت للمتداخل + القديم `/moon-today-in-{city}` سليم 200 بلا تحويل + Meeus ثابت.
+ اختبار مخصّص `scripts/_smoke_moon_city_year_route_structure_add_1.mjs` (MOON-CITY-YEAR-ROUTE-STRUCTURE-ADD-1): صفحة السنة 200 + `page-moon-year` + H1 واحد + canonical ذاتيّ + breadcrumb 5 مستويات DOM≡JSON-LD + جدول مراحل + 12 بطاقة شهر (روابط → **الشهر المتداخل الجديد `/moon/{country}/{city}/{yyyy}/{mm}`**) + سنة سابقة/تالية + 6 FAQ + hreflang + التحقّق + sitemap (سنة + أشهر موجودة) + Meeus ثابت.
+ اختبار مخصّص `scripts/_smoke_moon_city_month_route_structure_add_1.mjs` (MOON-CITY-MONTH-…-SCOPE-CORRECTION-FIX-1): صفحة الشهر 200 + **`page-moon` نشط (لا `page-moon-month`)** + 0 آثار bespoke (my-chip/my-day-link/moon-month-*) + breadcrumb 6 مستويات DOM≡JSON-LD + **شبكة التقويم القديمة `.moon-hub-cal-grid`** بروابط أيّام متداخلة (dim أو dim−1 بسبب خليّة «اليوم») + عنوان الشهر القديم + hreflang + التحقّق 404/301 + **الهب بلا تقويم** + التقويم لصفحة الشهر فقط (يوم/today/سنة/دولة بلا grid) + sitemap (أشهر موجودة، يوم غير موجود، today موجود) + legacy 301 + Meeus 49 (15 يونيو=المحاق، 30=البدر).
+ اختبار مخصّص `scripts/_smoke_moon_city_day_route_structure_add_1.mjs` (MOON-CITY-DAY-ROUTE-STRUCTURE-SCOPE-CORRECTION-FIX-1): صفحة اليوم 200 + **`page-moon` نشط (لا `page-moon-day`)** + H1 واحد + canonical ذاتيّ للرابط المتداخل (4 مدن/لغات، داخل النطاق) + **نفس محتوى الصفحة المؤرّخة القديمة** (نفس `<title>` + `#moon-page-h1` + جسم `#moon-city-answer`، **0** عناصر مخصّصة `page-moon-day`/`moon-day-hero`/`moon-day-summary`/`moon-day-details`/`moon-day-h1`) + خارج النطاق يبقى canonical ذاتيّ (لا يُحوَّل للقديم) + breadcrumb 7 مستويات DOM≡JSON-LD (AR+EN، رتبتا السنة/الشهر روابط متداخلة) + hreflang 10+x-default للرابط المتداخل + التحقّق (today/dash/`yyyy/6`/`yyyy/00`/`yyyy/13`/`yyyy/mm/7`/`yyyy/mm/00`/`yyyy/mm/32`/`yyyy/02/30`/أعمق/1899/2101 = 404، leap-aware: 2024/02/29=200 و2026/02/29=404 وApril/31=404، mismatch=301) + sitemap (**صفحات اليوم غير مُضافة دفعةً**، أشهر موجودة) + روابط الشهر صارت للمتداخل + الروابط القديمة سليمة (لا تحويل من القديم) + Meeus ثابت.
G) canonical: `/moon` ذاتيّ، مدن ذاتيّ، `/moon-today` بلا جسم/canonical، لا تكرار.
H) صفحات غير القمر ليست page-moon (SSR).
I) Meeus 49: الرياض 15/16/29/30 + مدينة أمريكيّة تعرض صفحة قمر + `_hijriForIana` للمدن + محرّك Meeus 49 موجود في moon.js.

أيّ كسر مستقبليّ لِبنية القمر يُسقِط هذا الاختبار قبل الدفع.
