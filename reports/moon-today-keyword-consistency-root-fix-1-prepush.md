# تقرير ما قبل الدفع: MOON-TODAY-KEYWORD-CONSISTENCY-ROOT-FIX-1

**النوع:** SEO — تنقية Keyword Consistency في `/moon-today` (إزالة أقسام placeholder من مسارات قمر أخرى تسرّب عناوين فارغة «—» إلى HTML الخام). **لم يُنفَّذ commit/push.**
**التحقّق:** SSR (node fetch، keyword counts + headings) + DOM-after-hydration (Preview، AR) + console errors + regression.

## 1) السبب الجذري
الصفحة `/moon-today` (الهب) تَشحن — ضمن نفس shell الـ SPA — **6 أقسام تعليمية/FAQ تخصّ مسارات القمر الأخرى** (مدينة-هب / شهر / تاريخ / مدينة-اليوم) وهي `display:none` على الهب (تظهر فقط على `html.moon-hub-page/.moon-date-page/.moon-month-page`). على الهب تبقى بنصّ placeholder «—»، فتُنتج في HTML الخام: **6 عناوين فارغة `<h2>—</h2>` + ~20 فقرة `<p>—</p>`**. زاحف SEOptimer يقرأ النصّ بصرف النظر عن `display:none`، فتظهر هذه العناوين الفارغة كضوضاء تُضعف Keyword Consistency.
**ملاحظة مهمّة:** الإصلاح السابق `MOON-TODAY-KEYWORD-CONSISTENCY-FIX-1` (2026-06-01) جرّد 12 page-wrapper خاملًا لكنه **فاته** هذه الأقسام الستّة من نفس العائلة. هذا الإصلاح يُكمل نفس النمط المُثبَت.

## 2) هل المشكلة من SEOptimer cache أم HTML/DOM حقيقي؟
**حقيقيّة جزئيًّا لكن محدودة.** جسم الصفحة **مركَّز على القمر فعلًا** (القمر ×76، إضاءة ×10، عمر القمر ×9، حالة القمر ×9 مقابل ضوضاء ضئيلة: مكة ×2، مواقيت الصلاة ×2، يونيو ×1). فالتركيز قويّ أصلًا؛ الخلل الوحيد المُثبَت هو الـ6 عناوين «—» الفارغة. (Title/Meta/H1 سليمة وداخل النطاق — لا تحتاج تعديلًا.)

## 3) هل يوجد SPA leak؟
**لا.** الصفحة النشطة الوحيدة `page-moon`؛ لا توجد wrappers أخرى نشطة (تم التحقّق SSR + DOM: `spaLeakWrappers=[]`). الإصلاح السابق لتجريد الـ12 wrapper ما زال فعّالًا.

## 4) جدول الكلمات قبل/بعد (جسم #page-moon)
| الكلمة | قبل | بعد |
|---|---|---|
| القمر | 76 | **76** (دون مساس) |
| إضاءة / عمر القمر / حالة القمر | 10 / 9 / 9 | **بلا تغيير** |
| مكة | 2 | 2 |
| مواقيت الصلاة | 2 | 2 |
| عناوين «—» الفارغة (h2) | **6** | **0** |
| إجمالي كلمات الجسم | 1075 | **1015** (حُذف ~60 كلمة «—» فقط) |
| إجمالي h2 في الهب | 17 | **11** (كلها قمريّة) |

## 5) Title/Meta/H1 قبل/بعد
| | قبل | بعد |
|---|---|---|
| Title | `حالة القمر اليوم: الطور الحالي ومراحل القمر والتقويم الهجري` (59) | **دون تغيير** (59) |
| Meta | (155، قمريّة) | **دون تغيير** (155) |
| H1 | `حالة القمر اليوم` | **دون تغيير** |
→ كلها سليمة وداخل النطاق أصلًا؛ لم تُمَسّ (لا keyword stuffing).

## 6) قائمة H2 قبل/بعد (الهب)
- **بعد (11، DOM):** القمر اليوم · طور القمر اليوم ونسبة الإضاءة · كيف تُقرأ بيانات القمر اليوم؟ · أطوار القمر خلال يونيو 2026 · مواعيد الأطوار القمريّة القادمة · موعد بدر قمر مكتمل والطور القادم · حسابات القمر حسب المدينة المختارة · حالة القمر اليوم في مدن أخرى · أسئلة شائعة عن القمر · أسئلة عامّة عن القمر · العد التنازلي للمناسبات الإسلامية (إبقاء مقصود سابق).
- **أُزيل:** 6 عناوين «—» الفارغة (city-hub-faq / city-hub-edu / month-edu / hub-related / today-city-edu / date-edu).

## 7) ما تم تقليله من noise
أُزيلت 6 أقسام placeholder «—» (≈60 كلمة فارغة + 6 عناوين فارغة) من HTML الخام للهب فقط. **لم تُزَل أيّ كلمة قمريّة** (القمر بقي 76). «مكة/مواقيت الصلاة» (2 لكلّ) لم تكن من هذه الأقسام وبقيت كما هي (سياق مشروع: المدينة الافتراضية للقمر + روابط).

## 8) ما تم إضافته من محتوى SSR-visible
**لا شيء جديد** — المحتوى التعليميّ القمريّ موجود وSSR-visible أصلًا (h2 «كيف تُقرأ بيانات القمر اليوم؟» ضمن moon-seo-info). تجنّبتُ الحشو عملًا بمبدأ «إصلاح السبب المثبت فقط».

## 9) هل حدث client overwrite؟
**لا.** DOM-after-hydration == SSR (Title 59 / Meta 155 / metaCount=1 محفوظة عبر الحارس العالميّ). لا أخطاء console (الأقسام المجرّدة route-gated؛ تعبئتها لا تعمل على الهب أصلًا).

## 10) الملفّات المعدَّلة (3 ملفّات، +24/-4)
| الملف | التغيير |
|---|---|
| `server.js` | +20 — إضافة 6 معرّفات أقسام cross-route إلى `_MOON_HUB_STRIP_IDS` (داخل `_stripHtmlForMoonHub`، المُستدعى فقط لـ `_isMoonTodayHub`) |
| `index.html` | `app.js?v=758 → 759` (موضعان) |
| `sw.js` | `CACHE_VERSION v418 → v419` + PRECACHE 759 |
- **لا تعديل app.js** (الإصلاح SSR-only؛ الأقسام route-gated). **لا تعديل CSS.**

## 11) تأكيد عدم تغيير الحسابات
✅ لم تُمَسّ: حسابات القمر / الهجري / الصلاة / القبلة / بيانات المدن. الإصلاح يجرّد عناوين placeholder فارغة فقط من الهب.

## 12) تأكيد canonical/og/url
✅ canonical `…/moon-today` (مضيف صحيح)، og:url مطابق، metaCount=1، hreflang/JSON-LD دون مساس.

## 13) نتائج regression
✅ **16/16 = 200** محليًّا (moon-today ar/en + city ar/en + medina + date + month + الرئيسية + hijri-calendar + hijri-month + hijri-date + today + msbaha + qibla-city + prayer-city + azkar). **صفحات مدينة/شهر/تاريخ القمر تحتفظ بأقسامها** (moon-today-city-edu / moon-date-edu-hijri / moon-month-edu-hijri حاضرة — التجريد للهب فقط).

## 14) cache-busters
`app.js?v=758 → 759` · `CACHE_VERSION v418 → v419` · PRECACHE 759.

## 15) رسالة commit المقترحة
```
seo(moon): MOON-TODAY-KEYWORD-CONSISTENCY-ROOT-FIX-1 — strengthen moon-today keyword focus and SSR content
```

---

## معايير القبول
1. لا SPA shell leak — ✅ (page-moon فقط)
2. Title ∈ 50–60 — ✅ (59، دون تغيير)
3. Meta ∈ 120–160 — ✅ (155، دون تغيير)
4. H1 واضح ويحمل النية — ✅ (حالة القمر اليوم)
5. Core moon keywords موزّعة — ✅ (Title/Meta/H1/H2/body؛ القمر ×76)
6. noise (مكة/مواقيت) لا تطغى — ✅ (×2 لكلّ مقابل القمر ×76؛ والآن صفر عناوين فارغة)
7. المحتوى التعليمي SSR-visible — ✅ (موجود أصلًا، لم يُحشَ)
8. DOM بعد hydration لا يدهس SSR — ✅
9. metaCount=1 — ✅
10. canonical/og:url سليمان — ✅
11. الحسابات لا تتغيّر — ✅
12. regression 200 — ✅ (16/16)
13. Keyword Consistency يتحسّن/قابل للتفسير بلا حشو — ✅ (إزالة 6 عناوين فارغة + توثيق هيمنة القمر ×76)

## ملاحظة شفّافة
هذا الإصلاح **منضبط لا جذريّ-جراحيّ**: التدقيق أثبت أنّ الصفحة مركَّزة على القمر أصلًا (Title/Meta/H1/الجسم)، فلم أُعِد كتابتها ولم أحشُ كلمات؛ عالجتُ السبب المثبت الوحيد (عناوين «—» الفارغة من أقسام مسارات أخرى). أُجِّل (اختياريّ، غير حاسم) قسم `moon-general-faq` المخفيّ ذو المحتوى القمريّ الحقيقيّ — يُترَك لأنه محتوى قمريّ مفيد لا «—» ضوضاء.

## تأكيدات
- تقرير ما قبل الدفع — لم يُنفَّذ commit/push. أوقفتُ خوادم الاختبار. صفر تعديل CSS/app.js/i18n/أذكار/الحسابات. لم تُبدأ أيّ صفحة أذكار.

**في انتظار اعتمادك للدفع:** `أعتمد دفع تقرير: MOON-TODAY-KEYWORD-CONSISTENCY-ROOT-FIX-1`
