# تقرير ما بعد الدفع: NEXT-PRAYER-CITY-MULTILANG-I18N-FIX-1

**الإنتاج:** https://prayer-times-d4w8.onrender.com · **commit:** `5178aba` (`_i18nVersion=197`, `CACHE_VERSION v440`).
**طريقة التحقّق:** **WebFetch** (مسار شبكة بديل) — `curl` في بيئة العمل ما زال محجوب TLS لـonrender.com (حادثة بنية موثّقة في تقرير MSBAHA postpush). WebFetch يقرأ HTML الـSSR محوَّلًا إلى نصّ.

## النشر
- دُفِع `0488847..5178aba`. أوّل فحص (بعد ~3.5 د) أظهر حالة v196 القديمة (Saudi Arabia / সাইট সেটিংস) ⇒ النشر لم يكتمل بعد. بعد إمهال إضافيّ، تأكّدت حالة v197.
- **دليل أنّ v197 حيّ:** على `/bn/next-prayer-in-riyadh` صارت الدولة «সৌদি আরب» (كانت «Saudi Arabia») وطريقة الحساب «সাইটের সেটিংস» (كانت «সাইট সেটিংস») — وكلاهما تغييرا SSR في server.js لا يظهران إلّا على v197.

## النتائج على الإنتاج
### 1) `/bn/next-prayer-in-riyadh`
| العنصر | النتيجة |
|---|---|
| الشارة (eyebrow) | ✅ «আজকের নামাজের সময়» موجودة (كانت عربيّة «مواقيت الصلاة اليوم» على v196) |
| المدينة | ✅ «রিয়াদ» (من البيانات) |
| الدولة | ✅ «সৌদি আরب» (بنغاليّ — كانت إنجليزيّة) |
| التوقيت | ✅ «Asia/Riyadh» (IANA سليم لم يُكسر) |
| طريقة الحساب | ✅ «সাইটের সেটিংস» (طبيعيّ — كانت نقحرة «সাইট সেটিংস») |

### 2) `/bn/next-prayer-in-macau`
- الدولة ✅ «চীন» (بنغاليّ — كانت «China») · المدينة ✅ «Macau» (من البيانات) · التوقيت «—» (فجوة `cn` سابقة موثّقة في تقرير ما قبل الدفع §7 — خارج نطاق i18n، لم تتغيّر).

### 3) `/en/next-prayer-in-riyadh`
- الدولة ✅ «Saudi Arabia» (صحيح للإنجليزيّة) · طريقة الحساب ✅ «Site settings» · العنوان الفرعيّ إنجليزيّ.

### 4) `/next-prayer-in-riyadh` (العربيّة)
- الدولة ✅ «المملكة العربية السعودية» · طريقة الحساب ✅ «حسب إعدادات الموقع» (عربيّ صحيح).

## ملاحظة عن أداة التحقّق (الشارة eyebrow)
ملخِّص WebFetch يرصد **بطاقات** (الدولة/التوقيت/الحساب) بدقّة، لكنّه لا يعزل **span الشارة القصير** المضمّن فوق الـH1 في تحويل markdown (يُعيد بدلًا منه الـ`<title>` أو شريط العدّ). لذلك:
- **bn:** الشارة مؤكَّدة على الإنتاج (سؤال مباشر «هل تظهر আজকের নামাজের সময়؟» = نعم).
- **en/بقيّة اللغات:** الشارة مُتحقَّق منها على **بايتات `5178aba` المحليّة المطابقة للمنشور** (فحص SSR لكلّ 10 لغات أظهر الشارة صحيحة: en=«Today’s Prayer Times»، fr=«Horaires de prière du jour»، ur=«آج کے نماز کے اوقات»، de=«Gebetszeiten heute»… إلخ). آليّة الـi18n walker نفسها المُثبَتة لـbn على الإنتاج تخدم بقيّة اللغات بالكامل (نفس الحزمة، نفس النشر).

## نقطة دقّة: ظهور «مواقيت الصلاة اليوم» في وثيقة الصفحة
الـSPA صفحة واحدة (index.html) تُرسَل كاملةً في كلّ مسار. عبارة «مواقيت الصلاة اليوم» العربيّة تظهر في **أقسام أخرى** من الوثيقة (روابط مدن الصفحة الرئيسيّة عبر `title`/`aria-label` الافتراضيّة، إلخ) — **سابقة وخارج نطاق هذه التذكرة**، وليست شارة الـNPT (التي صارت بنغاليّة كما تأكّد). لا علاقة لها بهذا الإصلاح ولا تُعدّ انحدارًا.

## ما تم الحفاظ عليه (مؤكَّد)
✅ منطق الصلاة القادمة + الأوقات · معرّف التوقيت IANA «Asia/Riyadh» سليم · أسماء المدن من البيانات (`রিয়াদ`/`Macau`) بلا ترجمة runtime · slugs/canonical/hreflang/sitemap/Title/Meta · صفحات الأذكار · العربيّة/الإنجليزيّة كأساس صحيح.

## الخلاصة
صفحة `/next-prayer-in-{city}` صارت تعرض **الشارة + اسم الدولة + طريقة الحساب** بلغة الصفحة على الإنتاج عبر اللغات؛ التوقيت يبقى معرّف IANA؛ أسماء المدن من البيانات؛ ولا مساس بالحسابات أو SEO. التصنيفات A/B/D/G **مغلقة** (التوقيت E مقبول؛ ماكاو «—» فجوة سابقة خارج النطاق).

**النتيجة المقترحة:** ✅ **PASSED** — جاهز للإغلاق: `اعتماد وإغلاق تقرير: NEXT-PRAYER-CITY-MULTILANG-I18N-FIX-1`

*(ملاحظة ثابتة: لا تُبدأ أيّ صفحة أذكار جديدة حتى اعتماد `/azkar/prayer-azkar` بصريًّا.)*
