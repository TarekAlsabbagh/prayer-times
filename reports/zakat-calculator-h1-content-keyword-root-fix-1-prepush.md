# تقرير ما قبل الدفع: ZAKAT-CALCULATOR-H1-CONTENT-KEYWORD-ROOT-FIX-1

**النوع:** إصلاح جذريّ لـ SEO على `/zakat-calculator` و`/[lang]/zakat-calculator` (10 لغات) — معالجة ثلاث مشكلات SEOptimer دفعةً واحدة: **H1 Header Tag Usage** (19 → 1)، **Amount of Content** (Word Count 558 → 1300+ للعربية/الإنجليزية و1000+ للبقية)، ومنع استباقيّ لمشكلة **Keyword Consistency**. **لم يُنفَّذ commit/push.**
**الطريقة المعتمدة (الخيار الأول):** نظام i18n متناسق — النصوص في `js/i18n/{lang}.js` + `js/i18n.js`، وSSR يبني الأقسام من المفاتيح (بلا نصوص طويلة مكرّرة في server.js)، مع رفع `_i18nVersion`.
**التحقّق:** SSR (curl، 10 لغات، port 8080 محليًّا) + قياس عددِ الكلمات/الكثافة + regression. الأساس: آخر main.

---

## 1) السبب الجذري المختصر
- **H1=19:** الـ SSR يشحن كلّ صفحات `.page` في DOM؛ كلّ صفحة تحمل `<h1>` خاصّتها، فيرى الزاحف 19 وسم H1 في الـ HTML الخام. صفحة الزكاة لم تكن مسجَّلة في `_getActiveH1Marker`، فلم تُخفَّض H1 الصفحات غير النشطة.
- **محتوى ضعيف:** المحتوى التعليميّ القديم على الصفحة `data-i18n` يُملأ في العميل (client-side)، فالـ HTML الخام الذي يقيسه SEOptimer كان فقيرًا (≈558–919 كلمة).
- **خطر Keyword Consistency:** غياب محتوى نصّيّ متنوّع يجعل أيّ تكرار بسيط يبدو مكثّفًا.

## 2) الحلّ
1. **H1:** تسجيل الزكاة في `_getActiveH1Marker` (`/^\/zakat-calculator$/ → {kind:'id', value:'zakat-h1'}`) + إضافة `id="zakat-h1"` لعنوان الهيرو ⇒ `_downgradeInactiveH1s` يخفض الـ18 H1 للصفحات غير النشطة إلى H2 في الـ HTML الخام ⇒ **H1=1**.
2. **المحتوى (SSR، مكمِّل لا مكرِّر):** باني محتوى داخل كتلة `_isZakatCalc` يقرأ مفاتيح i18n ويحقن في `<div id="zk-seo">` أقسامًا بطاقيّة (هويّة خضراء، فئات `.dconv-*`/`.hd1-*` المعاد استخدامها): **دليل الزكاة + تعريف «ما هي الزكاة وكيف تُحسب؟» + خطوات الاستخدام (4) + أمثلة عمليّة (5) + ملاحظات مهمّة (6) + روابط مفيدة (5)** — كلّها تكمِّل قسم النِصاب/أنواع الأموال/كيفية الحساب الموجود أصلًا على الصفحة (لا تكرّره).
3. **FAQ موسّع:** سؤالان جديدان q8/q9 (متى تُخرَج الزكاة؟ / هل تختلف زكاة الذهب عن النقد؟) في `<details>` بالصفحة + في FAQPage JSON-LD ⇒ **9 أسئلة** في المخطّط.
4. **Keyword Consistency:** توزيع طبيعيّ للكلمات المفتاحيّة (حاسبة الزكاة، زكاة المال، نصاب الزكاة، مقدار الزكاة، زكاة الذهب/الفضّة/النقد) عبر فقرات متنوّعة دون حشو.
5. **Title/Meta ladder:** ضبط العناوين/الأوصاف خارج النطاق إلى 50–60 / 120–160 لكلّ اللغات.

## 3) عدد الكلمات (HTML الخام داخل `#page-zakat` فقط — قياس متحفّظ يستثني الهيدر/الفوتر/الشريط الجانبيّ الذي يحسبه SEOptimer)
| اللغة | قبل (تدقيق التذكرة) | بعد | الهدف | الحالة |
|---|---|---|---|---|
| ar | 919 | **1306** | 1300+ | ✅ |
| en | 1043 | **1510** | 1300+ | ✅ |
| fr | — | **1686** | 1000+ | ✅ |
| tr | — | **1196** | 1000+ | ✅ |
| ur | — | **1662** | 1000+ | ✅ |
| de | — | **1397** | 1000+ | ✅ |
| id | — | **1313** | 1000+ | ✅ |
| es | — | **1660** | 1000+ | ✅ |
| bn | — | **1314** | 1000+ | ✅ |
| ms | — | **1326** | 1000+ | ✅ |
> ملاحظة: قياس SEOptimer يشمل كامل نصّ الصفحة (تنقّل + فوتر + شريط)، فالقيمة الفعليّة أعلى من أرقام الجدول.

## 4) H1 قبل/بعد
| | قبل | بعد |
|---|---|---|
| عدد `<h1>` في HTML الخام لمسار الزكاة | 19 | **1** |
| H1 الناجي | (غير محدَّد) | **`<h1 id="zakat-h1" class="zakat-hero-title">` (هيرو الزكاة)** |
- تأكيد على مسارات أخرى (الخام): `/`=H1 واحد، `/date-converter`=H1 واحد، `/hijri-calendar`=H1 واحد — سلوك `_downgradeInactiveH1s` سليم وغير متأثّر.

## 5) FAQ + JSON-LD (10 لغات)
- عدد أسئلة FAQPage JSON-LD = **9** (q1–q9) لكلّ اللغات العشر.
- نصوص q8/q9 حاضرة بالعربيّة (متى تُخرَج الزكاة / هل تختلف زكاة الذهب عن النقد) وبالإنجليزيّة (When is zakat paid / Is gold zakat different from cash zakat).
- HowTo JSON-LD: دون تغيير (gated بـ `zakatFaq`).

## 6) اختبار كلّ اللغات (10/10) — SSR
كلّها HTTP 200 · `#zk-seo` ممتلئ ✅ · `#page-zakat` هي active الوحيدة ✅ · `html.zakat-calculator-page` محقون ✅ · H1=1 ✅ · JSON-LD FAQ=9 ✅ · Title ضمن 50–60 ✅ · Meta ضمن 120–160 ✅.

## 7) Title/Meta ladder — القيَم بعد الضبط (طول بالأحرف)
| اللغة | Title | Meta | تعديل |
|---|---|---|---|
| ar | 52 | 130 | — (كان سليمًا) |
| en | 52 | 143 | Title 46→52 (Calculate Zakat on Money and Gold) |
| fr | 52 | 131 | Meta 114→131 (exemples inclus) |
| tr | 53 | 130 | — |
| ur | 52 | 127 | Meta 118→127 (آسانی سے) |
| de | 52 | 136 | Title 44→52 (Zakat auf Geld, Gold und Ersparnisse) |
| id | 50 | 133 | Title 43→50 (Hitung Zakat Mal, Emas dan Uang) |
| es | 50 | 125 | Title 48→50 (Calcula el Zakat del Dinero) |
| bn | 53 | 123 | Title 49→53 + Meta 110→123 |
| ms | 50 | 124 | — |
- جميعها الآن ضمن النطاق (Title 50–60، Meta 120–160).

## 8) Keyword Consistency — لقطة الكثافة (داخل `#page-zakat`)
**العربيّة (≈1323 كلمة):** حاسبة الزكاة 0.60% · زكاة المال 1.28% · نصاب 2.19% · مقدار الزكاة 0.68% · زكاة الذهب 0.53% · زكاة الفضّة 0.30% · زكاة النقد 0.23%.
**الإنجليزيّة (≈1537 كلمة):** zakat calculator 0.46% · zakat on money 0.65% · zakat amount 0.59% · nisab 1.76% · gold zakat 0.33% · silver zakat 0.20% · calculate zakat 0.07%.
> كلّها ضمن نطاق صحّيّ (<3%)، موزّعة على عبارات متعدّدة بلا هيمنة كلمة واحدة ⇒ يعالج Keyword Consistency استباقيًّا. الكلمات العامّة (على/بين/اليوم/المبلغ/القيمة/amount/value/today) لم تُضخَّم.

## 9) تأكيد عدم تغيير حساب الزكاة
✅ **منطق الحساب غير ممسوس.** `js/app.js` و`css/style.css` **خارج تغييرات هذه التذكرة** (تأكيد عبر `git status`: نظيفان). النموذج/الحقول/النتيجة كما هي؛ نِصاب 21,250 ر.س وإدخال 100,000 ⇒ 2,500 ر.س (2.5%) — منطق ثابت من تذاكر سابقة. أعدتُ استخدام فئات CSS موجودة (`.dconv-*`/`.hd1-*`، 21 قاعدة) ⇒ **بلا CSS جديد**.

## 10) الملفّات المعدَّلة (14 ملفًّا)
| الملف | التغيير | الأسطر |
|---|---|---|
| `server.js` | تسجيل H1 للزكاة + باني محتوى `#zk-seo` (دليل/تعريف/خطوات/أمثلة×5/ملاحظات×6/روابط×5) + توسيع FAQ JSON-LD لـ q8/q9 + Title/Meta ladder (8 قيَم) + رفع `_i18nVersion` 191→192 | +65/-12 |
| `index.html` | `id="zakat-h1"` للهيرو + `<div id="zk-seo">` + عنصرا FAQ q8/q9 | +15 |
| `js/i18n.js` | ~42 مفتاح zakat.* × 10 لغات + توسعة عمق + AR boost (SSR source) | +501 |
| `js/i18n/{ar..ms}.js` (×10) | نفس المفاتيح native لكلّ لغة (client) | +55–60/لغة |
| `sw.js` | `CACHE_VERSION v430→v431` | +1/-1 |
- **بلا مساس:** منطق/حقول/نتيجة الحاسبة، `js/app.js`، `css/style.css`، canonical/hreflang/sitemap، الأذكار. كلّ الملفّات LF. لا curated/بيانات.

## 11) المفاتيح الجديدة (نفس البنية للغات العشر، native لكلّها)
`zakat.guide.title/.text` · `zakat.zintro_title/_text` · `zakat.zsteps_title` + `zakat.zstep{1..4}_t/_d` · `zakat.zex_title/_intro` + `zakat.zex{1..5}_t/_b` · `zakat.znotes_title` + `zakat.znote{1..6}` · `zakat.zlinks_title/_intro` + `zakat.zlink{1..5}_label/_desc` · `zakat.faq.q8/a8/q9/a9`.

## 12) أقسام SSR التي تُبنى في `#zk-seo` (بنمط بطاقات الهويّة الخضراء)
بطاقة الدليل 🪙 → بطاقة «ما هي الزكاة» → قائمة خطوات مرقّمة (4) → شبكة أمثلة (5: مال/ذهب/مدّخرات/فضّة/عروض تجارة) → بطاقة ملاحظات تنبيهيّة (6) → شبكة روابط مفيدة (5: مواقيت/هجري اليوم/تقويم/تحويل/أذكار). + FAQ موسّع (q1–q9) أسفلها.

## 13) عزل المسارات (الإنتاج/المحليّ)
- `/` → `home-page` (مواقيت الصلاة active). `/date-converter` → `date-converter-page`. `/zakat-calculator` → `zakat-calculator-page`. الحقن مقصور بـ `_isZakatCalc` ⇒ **بلا تسرّب**.

## 14) regression (محليًّا، 11/11 = 200)
`/`، `/hijri-calendar`، `/today-hijri-date`، `/moon-today`، `/msbaha`، `/qibla-in-riyadh`، `/prayer-times-in-riyadh`، `/azkar`، `/date-converter`، `/zakat-calculator`، `/en/zakat-calculator` — كلّها 200. لا أخطاء بناء (`node --check` على server.js + i18n.js + ملفّات اللغات). i18n loader: «Loaded 10 languages — 1512 keys/lang».

## 15) cache-busters
- `_i18nVersion` 191→192 (يُجدِّد حُزَم `js/i18n/{lang}.js` للعملاء العائدين — حاملة المفاتيح الجديدة native للعشر لغات).
- `sw.js`: `CACHE_VERSION v430→v431`.
- لا تغيير في `app.js?v`/`style.css?v` (لم يتغيّرا في هذه التذكرة).

## 16) رسالة commit المقترحة
```
seo(zakat): ZAKAT-CALCULATOR-H1-CONTENT-KEYWORD-ROOT-FIX-1 — expand multilingual zakat content and fix H1 usage
```

## 17) معايير القبول
1. H1=1 على مسار الزكاة (10 لغات) — ✅ · 2. كلمات AR/EN ≥1300 + البقية ≥1000 — ✅ · 3. FAQ=JSON-LD=9 — ✅ · 4. Title 50–60 / Meta 120–160 (10 لغات) — ✅ · 5. توزيع كلمات مفتاحيّة صحّيّ بلا حشو — ✅ · 6. `#page-zakat` active وحيدة + `html.zakat-calculator-page` + عزل المسارات — ✅ · 7. حساب الزكاة (100,000 → 2,500) دون تغيير — ✅ (app.js/css غير ممسوسين) · 8. لا CSS جديد (إعادة استخدام `.dconv-*`/`.hd1-*`) — ✅ · 9. regression 11/11=200 — ✅ · 10. كلّ المحتوى native للعشر لغات — ✅ · 11. LF + لا curated — ✅.

## 18) تأكيدات
- لم يُنفَّذ commit/push. أوقفتُ خادم الاختبار. لم تُبدأ أيّ صفحة أذكار جديدة. حُذِفت ملفّات `.tmp_*` المؤقّتة. الملفّات المعدَّلة: `server.js` + `index.html` + `js/i18n.js` + 10 × `js/i18n/{lang}.js` + `sw.js` (+ هذا التقرير) فقط.
- **تنبيه عن التحقّق على الإنتاج:** لا يمكن فحص DOM-after-hydration للإنتاج عبر Preview (مقيَّد بـ localhost)؛ التحقّق بعد الدفع سيكون عبر curl SSR + إشارات النشر (sw v431 / i18n v192) بعد اكتمال نشر Render (~150–270s).

**في انتظار اعتمادك للدفع:** `أعتمد دفع تقرير: ZAKAT-CALCULATOR-H1-CONTENT-KEYWORD-ROOT-FIX-1`

*(ملاحظة ثابتة: لا تُبدأ أيّ صفحة أذكار جديدة حتى اعتماد `/azkar/prayer-azkar` بصريًّا.)*
